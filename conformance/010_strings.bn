package "main"

import "pkg/builtins/testing"

func main() {
	var s *[]readonly char = "hello"
	testing.Println(s)
	testing.Println(len(s))
	testing.Println(cast(int, s[0]))
	testing.Println(cast(int, s[4]))

	var c char = 'A'
	testing.Println(cast(int, c))
}
