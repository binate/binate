package "main"

import "pkg/builtins/testing"

type Point struct {
	x int
	y int
}

func main() {
	p := box(Point{x: 10, y: 20})
	testing.Println(p.x)
	testing.Println(p.y)
	p.x = 99
	testing.Println(p.x)
}
