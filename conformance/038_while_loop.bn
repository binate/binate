package "main"

func gcd(a int, b int) int {
	for b != 0 {
		t := b
		b = a % b
		a = t
	}
	return a
}

func main() {
	println(gcd(48, 18))
	println(gcd(100, 75))
	println(gcd(17, 13))

	// Infinite loop with break
	n := 1
	for {
		n = n * 2
		if n > 1000 {
			break
		}
	}
	println(n)
}
