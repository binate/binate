package "main"

import "pkg/builtins/testing"

type Rect struct {
	w int
	h int
}

func main() {
	var r @Rect = make(Rect)
	testing.Println(r.w)
	testing.Println(r.h)
	r.w = 5
	r.h = 10
	testing.Println(r.w * r.h)
}
