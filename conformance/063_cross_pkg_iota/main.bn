package "main"
import "pkg/builtins/testing"

import "pkg/color"

func main() {
	testing.Println(color.RED)
	testing.Println(color.GREEN)
	testing.Println(color.BLUE)
	testing.Println(color.Name(color.RED))
	testing.Println(color.Name(color.GREEN))
	testing.Println(color.Name(color.BLUE))
}
