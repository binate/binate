package "pkg/color"

const (
	RED int = iota
	GREEN
	BLUE
)

func Name(c int) []char {
	if c == RED { return "red" }
	if c == GREEN { return "green" }
	if c == BLUE { return "blue" }
	return "unknown"
}
