package "main"

import "pkg/builtins/testing"

// Test slice-of-slices: *[]*[]readonly char operations.
// Exercises struct-sized element append, get, set, and len.

func main() {
	var ss @[]*[]readonly char = make_slice(*[]readonly char, 3)
	ss[0] = "hello"
	ss[1] = "world"
	ss[2] = "test"

	testing.Println(len(ss))
	for i := 0; i < len(ss); i++ {
		testing.Println(ss[i])
	}

	// Overwrite an element
	ss[1] = "replaced"
	testing.Println(ss[1])

	// Nested: slice of slices built from variables
	var a *[]readonly char = "alpha"
	var b *[]readonly char = "beta"
	var items @[]*[]readonly char = make_slice(*[]readonly char, 2)
	items[0] = a
	items[1] = b
	testing.Println(items[0])
	testing.Println(items[1])
}
