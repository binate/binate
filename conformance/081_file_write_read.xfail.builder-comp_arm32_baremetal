Exercises bootstrap.Open / Write / Read against the filesystem. ARM32 bare-metal has no filesystem — the bare-metal pkg/builtins/bootstrap stubs Open returns -1 — so this test can never pass under --target arm32-baremetal by design.
