package "main"

import "pkg/builtins/testing"

func main() {
	x := 0xFF
	x &= 0x0F
	testing.Println(x)

	y := 0xF0
	y |= 0x0F
	testing.Println(y)

	z := 0xFF
	z ^= 0x0F
	testing.Println(z)

	a := 1
	a <<= 4
	testing.Println(a)

	b := 32
	b >>= 2
	testing.Println(b)
}
