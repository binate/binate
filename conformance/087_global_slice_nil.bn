package "main"

import "pkg/builtins/testing"

var items @[]int

func setItems() {
	items = make_slice(int, 3)
	items[0] = 10
	items[1] = 20
	items[2] = 30
}

func resetItems() {
	var zero @[]int
	items = zero
}

func main() {
	// Global slice starts as zero value
	testing.Println(len(items))

	setItems()
	testing.Println(len(items))
	testing.Println(items[0])
	testing.Println(items[2])

	// Reset to zero value
	resetItems()
	testing.Println(len(items))

	// Re-use after reset
	items = make_slice(int, 1)
	items[0] = 99
	testing.Println(len(items))
	testing.Println(items[0])
}
