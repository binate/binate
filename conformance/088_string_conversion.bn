package "main"

func printChars(s *[]readonly char) {
	for i := 0; i < len(s); i++ {
		println(cast(int, s[i]))
	}
}

func main() {
	// String literal to *[]readonly char
	var s *[]readonly char = "Hi"
	println(len(s))
	printChars(s)

	// Reassign string to *[]readonly char variable
	s = "AB"
	println(len(s))
	printChars(s)

	// Empty string
	var e *[]readonly char = ""
	println(len(e))

	// *[]readonly char from function return used as string
	println(s)
}
