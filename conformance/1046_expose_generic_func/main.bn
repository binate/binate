package "main"

import "pkg/fwd"

// Exposed generic FUNC referenced through the forwarder spelling: fwd.Ident[int]
// must monomorphize/mangle to its HOME (pkg/glib.Ident[int]) — the generic-decl
// lookup keys on the home, not the forwarder alias.
func main() {
	println(fwd.Ident[int](7)) // 7
}
