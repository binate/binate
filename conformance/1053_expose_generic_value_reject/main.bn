package "main"

import "pkg/fwd"

// An exposed generic type name used as a VALUE (`fwd.Box` with no type arguments,
// in an rvalue position) is rejected — the marker is not a value, and must not
// yield its nil placeholder Type.
func main() {
	var x = fwd.Box
	println(0)
}
