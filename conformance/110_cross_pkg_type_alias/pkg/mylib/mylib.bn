package "pkg/mylib"

type Result = @[]char

func MakeResult(s []char) Result {
	var r @[]char = make_slice(char, len(s))
	for i := 0; i < len(s); i++ {
		r[i] = s[i]
	}
	return r
}

func IsEmpty(r Result) bool {
	return r == nil || len(r) == 0
}
