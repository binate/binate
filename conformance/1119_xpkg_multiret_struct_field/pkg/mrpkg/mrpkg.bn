package "pkg/mrpkg"

func mkStErr(fail bool) (St, @any) {
	var s St
	s.flag = true
	s.a = 7
	s.b = 9
	var noErr @any
	if fail {
		var boxed @int = box(1)
		return s, boxed
	}
	return s, noErr
}

func mkStInt(x int) (St, int) {
	var s St
	s.a = x
	s.b = x + 1
	return s, x * 10
}
