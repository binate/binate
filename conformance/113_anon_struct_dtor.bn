package "main"

// Anonymous struct with a managed pointer field.
// Requires a destructor for correct cleanup.
func main() {
	type Inner struct { X int }
	var s struct { Val int; Ptr @Inner }
	s.Val = 42
	s.Ptr = make(Inner)
	s.Ptr.X = 99
	println(s.Val)
	println(s.Ptr.X)
}
