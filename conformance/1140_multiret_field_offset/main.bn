package "main"
import "pkg/builtins/testing"

import "pkg/mrfpkg"

func main() {
	var a int32
	var p mrfpkg.P
	a, p = mrfpkg.mkIntP()
	testing.Println(cast(int, a))   // 99
	testing.Println(cast(int, p.x)) // 10  (garbage before the fix: aa64 P.x, x64 P.y)
	testing.Println(cast(int, p.y)) // 20

	var t mrfpkg.Tri
	var n int8
	t, n = mrfpkg.mkTriByte()
	testing.Println(cast(int, t.a)) // 11
	testing.Println(cast(int, t.b)) // 22
	testing.Println(cast(int, t.c)) // 33
	testing.Println(cast(int, n))   // 44
}
