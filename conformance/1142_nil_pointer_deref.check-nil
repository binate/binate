Enables `bni --check-nil` on the int (bytecode-VM) runners so this nil deref
raises a recoverable VM fault instead of a SEGV.  Opt-in nil-pointer-deref
checking is VM-only and off by default; see plan-nil-check-opt-in.md (N3).
