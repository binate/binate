package "main"

// Anonymous struct as a struct field type.

type Wrapper struct {
	Inner struct { X int; Y int }
	Tag   int
}

func main() {
	var w @Wrapper = make(Wrapper)
	w.Inner.X = 10
	w.Inner.Y = 20
	w.Tag = 99
	println(w.Inner.X)
	println(w.Inner.Y)
	println(w.Tag)
}
