package "pkg/mp"

// Box[T] is body-included (its method body lives here in the .bni) so a
// cross-package consumer can monomorphize it and take a method EXPRESSION
// off the instantiation (`mp.Box[int].Get`).
type Box[T any] struct {
	val T
}

func (b *Box[T]) Get() T {
	return b.val
}
