package "pkg/b"

import "pkg/std/mapfn"

func bHash(k int) uint { return cast(uint, k) }

func bEq(x int, y int) bool { return x == y }

func Count() int {
	var m @mapfn.MapFn[int, int] = mapfn.NewMapFn[int, int](bHash, bEq)
	m.Put(1, 10)
	m.Put(2, 20)
	return m.Len()
}
