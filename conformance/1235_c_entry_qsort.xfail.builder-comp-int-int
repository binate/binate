The bytecode VM does not do FFI, and never will — it is a permanent non-goal, NOT a pending feature.  Code that needs C functionality in an interpreted context supplies a Binate impl via package-level injection instead of calling C, so a __c_entry/__c_call test can never pass in a VM-interpreted mode by design.  (This is not awaiting a VM FFI implementation; there will never be one.)
