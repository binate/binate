Bare-metal has no libc, so __c_call to a libc function (here qsort) can never link or run under --target arm32-baremetal — by design.  Anything needing the equivalent on bare-metal supplies a Binate impl via package-level injection, not a C call.  (This is not awaiting an implementation.)
