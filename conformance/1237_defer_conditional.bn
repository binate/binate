package "main"

import "pkg/builtins/testing"

// A conditional defer runs iff its statement executed (the Go idiom
// `if cond { defer ... }`).
func f(c bool) {
	if c {
		defer testing.Println(1)
	}
	testing.Println(0)
}

func main() {
	f(true)
	f(false)
}
