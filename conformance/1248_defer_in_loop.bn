package "main"

import "pkg/builtins/testing"

func g() {
	testing.Println(0)
}

// A defer lexically inside a for loop is a compile error (§14.13
// stmt.defer.no-loop).
func main() {
	for i := 0; i < 3; i = i + 1 {
		defer g()
	}
}
