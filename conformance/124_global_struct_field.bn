package "main"

type Config struct {
	Width  int
	Height int
	Debug  bool
}

var cfg Config

func initConfig() {
	cfg.Width = 800
	cfg.Height = 600
	cfg.Debug = true
}

func main() {
	initConfig()
	println(cfg.Width)
	println(cfg.Height)
	if cfg.Debug {
		println(1)
	} else {
		println(0)
	}
}
