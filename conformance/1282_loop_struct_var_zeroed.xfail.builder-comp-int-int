VM bug: a struct local declared without an initializer inside a loop body is not re-zeroed on each iteration (it keeps the previous iteration's value); scalars are fine. Tracked in claude-todo MAJOR.
