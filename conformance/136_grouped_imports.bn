package "main"

import (
	"pkg/rt"
	"pkg/buf"
)

func main() {
	// Verify both imports work
	var p *uint8 = rt.Alloc(8)
	println(rt.Refcount(p))
	rt.Free(p)

	var b buf.CharBuf = buf.New()
	b = buf.WriteByte(b, 'O')
	b = buf.WriteByte(b, 'K')
	println(buf.Bytes(b))
}
