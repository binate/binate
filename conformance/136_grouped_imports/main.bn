package "main"

import (
	"pkg/greet"
	"pkg/rt"
)

func main() {
	// Both names imported via the grouped form must resolve.
	var p *uint8 = rt.Alloc(8)
	println(rt.Refcount(p))
	rt.Free(p)

	println(greet.Hello())
}
