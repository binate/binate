package "main"
import "pkg/builtins/testing"

import (
	"pkg/greet"
	"pkg/builtins/rt"
)

func main() {
	// Both names imported via the grouped form must resolve.
	var p *uint8 = rt.Alloc(8)
	testing.Println(rt.Refcount(p))
	rt.Free(p)

	testing.Println(greet.Hello())
}
