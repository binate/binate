package "pkg/greet"

// Hello returns a fixed greeting. Used by the grouped-imports test as a
// second importable package alongside the whitelisted pkg/builtins/rt, so the test
// can exercise the parenthesized import form without pulling in unrelated
// toolchain libraries.
func Hello() *[]readonly char {
	return "OK"
}
