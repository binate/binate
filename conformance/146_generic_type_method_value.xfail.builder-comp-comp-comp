method-value on generic instantiation: captured-receiver struct is named with the raw source spelling 'Box[int]' (invalid LLVM identifier) instead of the mangled instantiation name — pre-existing mangling defect in gen_method_value.bn (checker type name used verbatim). Passes under the VM. See claude-todo.
