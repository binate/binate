package "main"

// Short var decl (:=) with untyped int literal should default to int.

func main() {
	x := 42
	println(x)

	// Arithmetic with short-var int should work normally
	y := x + 10
	println(y)

	// Boolean short-var
	b := true
	if b {
		println(1)
	}
}
