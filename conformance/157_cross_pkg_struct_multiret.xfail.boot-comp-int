VM corrupts struct value across cross-pkg multi-return destructure on the 2nd+ call. main.bn does `c, prev = counter.Bump(c, ...)` twice; the 2nd destructure clobbers c (and prev), so subsequent reads of c return garbage addresses. 1st destructure and a non-multi-return path are fine.
