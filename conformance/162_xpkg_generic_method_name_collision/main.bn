package "main"
import "pkg/builtins/testing"

import "pkg/coll"

func main() {
	var bx @coll.Box[int] = make(coll.Box[int])
	bx.Set(3)
	var bg @coll.Bag[int] = make(coll.Bag[int])
	bg.Set(4)
	testing.Println(bx.Get() + bg.Get())
}
