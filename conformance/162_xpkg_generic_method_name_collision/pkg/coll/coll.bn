package "pkg/coll"

// A same-named generic-receiver method body in the .bn (dual with the .bni)
// must suppress ONLY Box.Get in the .bni→merged pairing — not the unrelated
// Bag.Get (which sameFuncDecl must not false-match by empty base name).

func (b @Box[T]) Get() T {
	return b.v
}
