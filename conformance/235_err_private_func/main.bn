package "main"
import "pkg/builtins/testing"

import "pkg/mylib"

func main() {
	// This should work — PublicFunc is in .bni
	testing.Println(mylib.PublicFunc())

	// This should fail — privateHelper is NOT in .bni
	testing.Println(mylib.privateHelper())
}
