package "main"

import "pkg/mylib"

func main() {
	// This should work — PublicFunc is in .bni
	println(mylib.PublicFunc())

	// This should fail — privateHelper is NOT in .bni
	println(mylib.privateHelper())
}
