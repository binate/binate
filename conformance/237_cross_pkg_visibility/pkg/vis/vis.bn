package "pkg/vis"

// Private helper — not in .bni
func addInts(a int, b int) int {
	return a + b
}

func NewPoint(x int, y int) Point {
	var p Point
	p.X = x
	p.Y = y
	return p
}

func Sum(p Point) int {
	return addInts(p.X, p.Y)
}
