package "pkg/lo"

// Pos mimics token.Pos shape: a managed slice followed by ints.
// As an inline struct field in a cross-package type, its size
// differs sharply from an int fallback.
type Pos struct {
	File @[]char
	Line int
	Col  int
}
