Fails in boot-comp-int-int (bni-running-bni double-VM mode); passes in boot-comp-int and boot-comp-comp-int. The original `*[]T` raw-slice migration (plan-raw-slice-syntax.md Stage 1) is complete, so pkg/parser does handle the syntax. Root cause for this specific double-VM failure path is unknown — possibly a VM-on-VM resource or correctness issue surfaced by the test's pkg/parser exercise. The test takes ~245s before failing, suggesting it's running, not crashing immediately. Investigate when boot-comp-int-int returns to active maintenance (it's not in the default `all` modeset).
