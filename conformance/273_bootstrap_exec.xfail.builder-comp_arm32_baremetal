Exercises bootstrap.Exec (fork + execvp).  ARM32 bare-metal has no process model — the bare-metal pkg/builtins/bootstrap stubs Exec returns -1 — so this test can never pass under --target arm32-baremetal by design.
