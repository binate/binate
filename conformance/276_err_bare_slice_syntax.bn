package "main"

// Bare "[]T" as a type is no longer valid — raw slices must be written "*[]T"
// under the revised slice syntax (plan-raw-slice-syntax.md, Stage 3).
func main() {
	var s []int
	println(len(s))
}
