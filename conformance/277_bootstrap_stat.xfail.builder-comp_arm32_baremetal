Exercises bootstrap.Stat (filesystem inspection).  ARM32 bare-metal has no filesystem — the bare-metal pkg/bootstrap stubs Stat returns 0 — so this test can never pass under --target arm32-baremetal by design.
