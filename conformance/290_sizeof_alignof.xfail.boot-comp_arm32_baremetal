Expected output pins LP64-specific sizes (sizeof(int)=8, sizeof(*[]int)=16, sizeof(@[]int)=32, etc.).  Under --target arm32-baremetal the same expressions evaluate to ILP32 sizes (sizeof(int)=4, sizeof(*[]int)=8, sizeof(@[]int)=16), so the actual output diverges from the expected.  The test logic itself is correct on both targets — it's the .expected file that's LP64-pinned.  See claude-todo.md "TODO: substitute LP64-pinned conformance tests with target-aware variants" for the planned mechanism.
