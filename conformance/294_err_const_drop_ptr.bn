package "main"

// Dropping const on a pointer's element type is a capability leak
// and must be rejected. Use `cast` to opt out explicitly.

func main() {
	var x int = 5
	var p *readonly int = &x
	var q *int = p        // ← drop of element const; should error.
	_ = q
}
