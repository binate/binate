package "main"

// Writing through a pointer-to-const must be rejected. The LHS
// *p has type `const int`, which is a const-typed location.

func main() {
	var x int = 5
	var p *const int = &x
	*p = 10         // ← assigning to a const-typed location; should error.
}
