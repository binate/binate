package "main"

// Slice expression with hi > len(s) must trap at runtime.

func main() {
	var s @[]int = make_slice(int, 3)
	// len(s) == 3, so hi=5 is out of range.
	var t *[]int = s[0:5]
	println(len(t))
}
