package "main"

// Slice expression with lo > hi must trap. Captured by the lo-vs-hi+1
// bounds check.

func main() {
	var s @[]int = make_slice(int, 5)
	// hi=1, lo=3: lo > hi.
	var t *[]int = s[3:1]
	println(len(t))
}
