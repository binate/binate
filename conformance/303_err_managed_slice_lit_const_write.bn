package "main"

// Writing through a `@[]const T` is a capability error — already
// covered by Stage 1 const enforcement. Re-exercise via a composite
// literal entry point.

func main() {
	var s @[]const int = @[]const int{1, 2, 3}
	s[0] = 99   // ← element is const; rejected.
	_ = s
}
