package "main"

// Out-of-bounds indexing on a fixed-size array must trap at runtime.
// The bounds check uses the compile-time length.

func main() {
	var a [3]int
	a[0] = 100
	a[1] = 200
	a[2] = 300
	// Valid indices are 0..2. a[5] is out of range.
	println(a[5])
}
