package "main"

import "pkg/builtins/testing"

// Out-of-bounds index assignment must trap at runtime, mirroring the
// read-side check.

func main() {
	var s @[]int = make_slice(int, 2)
	s[0] = 1
	s[1] = 2
	s[7] = 99
	testing.Println("unreachable")
}
