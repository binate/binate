package "main"

import "pkg/builtins/bootstrap"

// Smallest program that exercises OP_CONST_INT + OP_CALL on the native
// backend: exits with a specific code via the C runtime's exit(3).

func main() {
	bootstrap.Exit(0)
}
