package "main"

// Function-local `type` declarations are rejected at parse time.
// `type` is a top-level-only declaration. See claude-notes.md
// § Scoping rules and differences-with-go.md.

func main() {
	type Local int  // parse error
	var x Local = 5
	println(x)
}
