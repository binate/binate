package "main"
import "pkg/builtins/testing"

// Function-local `type` declarations are rejected at parse time.
// `type` is a top-level-only declaration.

func main() {
	type Local int  // parse error
	var x Local = 5
	testing.Println(x)
}
