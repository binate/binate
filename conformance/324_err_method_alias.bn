package "main"

// Methods can only be defined on named types. Type aliases are NOT
// distinct types, so they cannot have methods. (claude-notes.md:555;
// plan-receivers.md decision 7.)
type MyInt = int

func (m MyInt) Get() int {
	return cast(m, int)
}

func main() { }
