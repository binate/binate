package "main"

// One method per (receiver-base-type, name) pair — re-declaring a
// method on the same type is an error. (plan-receivers.md decision 6.)
type Point struct { x int }

func (p *Point) X() int {
	return p.x
}

func (p *Point) X() int {
	return 0
}

func main() { }
