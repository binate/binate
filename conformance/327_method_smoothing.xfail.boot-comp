Receiver smoothing (*T expr on T method) is not yet emitted by the
LLVM IR-gen path; Stage 5 follow-up will add it. Currently passes on
boot (interpreter auto-deref) and boot-comp-int (VM tolerant of type
mismatch); fails on boot-comp because LLVM's type system rejects the
call.
