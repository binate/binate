`bit_cast(int, float64)` rewrite to `bit_cast(int64, ...)` makes the test compile on ILP32 (the original LP64 issue is gone), but `println(int64)` on arm32-baremetal hangs — likely an AEABI long-divide helper missing from the runtime impl-path, or a digit-extraction loop that doesn't terminate.  Separate runtime issue from the LP64 type-size mismatch; pin this here until the int64-print path on bare-metal is sound.
