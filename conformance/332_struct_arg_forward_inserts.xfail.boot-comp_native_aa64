cluster A native bug: codegen for forwarding >16-byte value-struct via stack reuses temp register as both source pointer and load destination, chasing through the loaded values instead of reading struct fields
