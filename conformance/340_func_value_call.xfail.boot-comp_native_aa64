macOS ld rejects the third vtable's call-slot relocation: "pointer
not aligned in '_bn_main__add__vt'+0x28". Vtable construction is
correct under boot-comp / boot-comp-int / boot-comp-comp; only the
3-vtables-within-a-single-test arm64 link fails. With 2 vtables
(test 339) the same machinery works. Function-pointer alignment in
the data-section relocation handling needs investigation — likely
either pkg/asm/macho's section file alignment or a target-address-
alignment requirement that ARM64_RELOC_UNSIGNED carries on macOS
that doesn't surface in the 2-vtable case. Tracked separately;
within-mode conformance passes 286/286 in compiled / VM /
self-compilation.
