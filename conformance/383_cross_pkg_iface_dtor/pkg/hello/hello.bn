package "pkg/hello"

import "pkg/greeter"

func (h *Holder) tag() int {
	return h.Tag
}

impl *Holder : greeter.Tagged

func MakeHolder(inner @Inner) @Holder {
	var h @Holder = make(Holder)
	h.Inner = inner
	h.Tag = 42
	return h
}
