VM stores iv[1] as a 0-based vtable index, so a zero-inited iv silently dispatches through vm.IfaceVtables[0] instead of panicking on nil. Should switch to 1-based to match compiled-mode SEGV semantics.
