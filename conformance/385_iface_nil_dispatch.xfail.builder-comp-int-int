VM-mode raises a recoverable fault with a message ("runtime error: call of nil interface value") rather than crashing silently, so the compiled-mode .expected = "before" doesn't match. VM-mode behavior is pinned by 386.
