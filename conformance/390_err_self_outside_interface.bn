package "main"

// `Self` is only valid inside an
// interface method's argument / result types.  A reference
// in any other position — here, a top-level func decl's
// parameter — surfaces as a clean type error.

func bad(x Self) bool {
	return true
}

func main() {
	if bad(true) {
		println("ok")
	}
}
