package "main"

import "pkg/builtins/testing"

// Negative: a parent interface listed twice in the same `:` clause is
// rejected at type-check time.

interface P {
	foo()
}

interface A : P, P {}

func main() {
	testing.Println("ignored")
}
