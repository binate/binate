package "main"

// Negative case: the right-hand side of `interface X : ...` must name an
// interface. A struct (or any non-interface type) is rejected at type-check
// time.

type S struct {
	x int
}

interface A : S {
}

func main() {
	println("ignored")
}
