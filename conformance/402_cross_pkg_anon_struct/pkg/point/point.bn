package "pkg/point"

// MakePoint returns an anonymous struct.  Its return type doesn't
// name anything from this package — the structural shape itself
// crosses the package boundary.
func MakePoint(x int, y int) struct { X int; Y int } {
	var p struct { X int; Y int }
	p.X = x
	p.Y = y
	return p
}
