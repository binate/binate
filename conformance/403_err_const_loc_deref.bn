package "main"

// Writing through a pointer-to-const must be rejected at type check,
// even though the underlying location is not itself declared const.
// The const-ness is on the path the write takes, not the storage.

func main() {
	var x int = 5
	var p *const int = &x
	*p = 7
	println(x)
}
