package "main"

// A raw slice of `const T` (`*[]readonly T`) is a read-only view of
// the backing storage. Writing through it must be rejected.

func main() {
	var a [3]int
	a[0] = 1
	var s *[]readonly int = a[:]
	s[0] = 99
	println(a[0])
}
