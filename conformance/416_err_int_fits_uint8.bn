package "main"

// Per claude-notes.md § "Integer literal value range and constant-
// expression arithmetic — DECIDED 2026-05-15", an integer literal
// must fit the target type's range.  256 doesn't fit uint8
// (range [0, 255]).

func main() {
	var x uint8 = 256
	println(cast(int, x))
}
