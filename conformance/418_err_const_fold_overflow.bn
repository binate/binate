package "main"

// `200 + 100 = 300` doesn't fit uint8; rejected at type-check time
// after const-folding produces 300 as the assigned value.

func main() {
	var x uint8 = 200 + 100
	println(cast(int, x))
}
