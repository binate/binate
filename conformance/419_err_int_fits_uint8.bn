package "main"

import "pkg/builtins/testing"

// An integer literal must fit the target type's range.  256 doesn't
// fit uint8 (range [0, 255]).

func main() {
	var x uint8 = 256
	testing.Println(cast(int, x))
}
