package "main"

// End-to-end smoke: a generic function declaration that is never
// called.  Type-checks under the generic decl rules and is skipped
// by IR-gen (generic decls produce no IR until monomorphization).
// Program must compile and run cleanly with just the non-generic
// caller in main.

func id[T any](x T) T {
	return x
}

func main() {
	println("ok")
}
