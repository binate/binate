package "main"

// Cross-package methods on generic types: main instantiates an imported
// generic type Box[int] and calls its methods.

import "pkg/gbox"

func main() {
	var b @gbox.Box[int] = make(gbox.Box[int])
	b.Set(42)
	println(b.Get())
}
