package "pkg/gbox"

// A generic type with methods, defined in a .bni (bodies included) so a
// cross-package consumer can monomorphize the methods at its use sites.

type Box[T any] struct {
	val T
}

func (b @Box[T]) Set(v T) {
	b.val = v
}

func (b @Box[T]) Get() T {
	return b.val
}
