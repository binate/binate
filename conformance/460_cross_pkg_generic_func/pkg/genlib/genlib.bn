package "pkg/genlib"

// Concrete (non-generic) helper to give the package something
// to compile on its own — the .bni's Id[T] is body-included so
// the cross-package consumer can monomorphize there.

func Sentinel() int {
	return 1
}
