package "main"

// `any` is an interface name, not a type expression.  Bare `any`
// as a type is rejected
// (use `*any` or `@any`).  Same rule as for user-declared
// interfaces.

func main() {
	var x any
	_ = x
}
