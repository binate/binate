package "main"

// A type error must be reported with its source location
// ("file:line:col:"), not just a bare message — otherwise large
// packages give no hint where the offending code is.  The undefined
// reference below is on line 9.
func main() {
	var x int = 1
	x = Nonexistent
	println(x)
}
