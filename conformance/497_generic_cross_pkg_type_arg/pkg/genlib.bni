package "pkg/genlib"

// Body-included so a cross-package consumer monomorphizes at the
// call site.
func Append[T any](s @[]T, v T) @[]T {
	var n int = len(s)
	var ns @[]T = make_slice(T, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}
