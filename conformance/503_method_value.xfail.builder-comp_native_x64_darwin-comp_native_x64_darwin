B.4 first cut: pointer-receiver method value works on aarch64 native but x64-darwin (Rosetta) shows partial output — likely a x64 shim emission edge case for the synthesized wrapper Func.  Investigate alongside B.4 value-receiver follow-up.
