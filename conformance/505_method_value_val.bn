package "main"

// Method value with value-T receiver (struct with one int field).

type Counter struct {
	N int
}

func (c Counter) Get() int {
	return c.N
}

func main() {
	var c Counter = Counter{N: 42}
	var f *func() int = c.Get
	println(f())  // 42
}
