B.4 first cut: value-T-receiver wrapper hits a VM-specific crash in the wrapper's OP_CALL to the underlying method.  Debug isolation: stub wrapper (returns 0 without calling method) works; restoring the OP_CALL crashes.  Needs deeper BC-trace investigation — likely a subtle struct-arg packing issue.  LLVM works.
