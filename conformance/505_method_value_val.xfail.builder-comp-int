B.4 first cut: value-T-receiver wrapper hits the VM/native struct-by-value calling convention mismatch; LLVM works.  Follow-up: thread the struct-by-pointer ABI through the wrapper's hand-built body.
