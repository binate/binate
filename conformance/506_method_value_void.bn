package "main"

// Minimal reproducer: void-return method value via pointer receiver.

type C struct {
	N int
}

func (c *C) Bump(by int) {
	c.N = c.N + by
}

func main() {
	var c C = C{N: 10}
	var cp *C = &c
	var b *func(int) = cp.Bump
	b(1)
	println(c.N)  // 11
}
