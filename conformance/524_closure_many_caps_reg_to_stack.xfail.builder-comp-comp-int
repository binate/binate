b1 (thunk-dispatch) routes a VM-closure call through its vtable.call trampoline, whose shim ABI caps at 7 user args; this closure has >7. To be un-xfail'd by the cross-mode >7-arg-limit fix (claude-todo); the compiled lanes (native/LLVM shim spill) still pass.
