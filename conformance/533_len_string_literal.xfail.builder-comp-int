len() on a bare string literal lowers the literal as a bare data pointer instead of a {ptr,len} slice, so the length extract is invalid IR (compiled backends) / a wrong value (VM). MAJOR — see claude-todo.
