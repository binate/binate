VM reports 'call through nil interface value' dispatching a present @Foo whose receiver (Holder) has a managed @Inner field — pre-existing VM bug (same as 520/385). Refcount balance is verified in builder-comp / -comp-comp / native.
