VM crashes running a struct dtor that recurses into an @Iface-field dtor whose receiver has its own managed field (the nested struct-dtor -> OP_IFACE_DTOR -> receiver-dtor chain). Codegen is correct — builder-comp / -comp-comp / native balance. Pre-existing VM iface limitation (see claude-todo); refcount balance verified in the non-VM modes.
