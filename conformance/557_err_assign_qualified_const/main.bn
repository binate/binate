package "main"

import "pkg/k"

// Assigning to a package-qualified const has no storage to write — must
// be rejected, like the unqualified `C = v` and the `&pkg.C` address-of.
// Without the check it silently lowers to a no-op.
func main() {
	k.C = 5
}
