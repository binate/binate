native aa64: the managed-ptr-copy fix is verified in the default modes, but the native-aa64 self-host lane currently fails to BUILD (duplicate-symbol link error, tracked separately), so this test can't run there.
