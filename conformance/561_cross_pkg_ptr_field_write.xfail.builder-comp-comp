field write through an imported ptr/struct var (pkg.G.V = v) silently no-ops: genSelectorPtr's qualified branch handles only e.X == EXPR_IDENT, not a nested selector base. Fix: a qualified-var-base case in genSelectorPtr.
