native x64-darwin only: a float captured/passed in a closure reads the wrong value — the closure shims (emitClosureShim*) lack float GP<->FP handling, same family as the aa64 xfail. Pre-existing (a wrong-answer before the all-int func-value shim work too); tracked under claude-todo #120 as the closure-float follow-up.
