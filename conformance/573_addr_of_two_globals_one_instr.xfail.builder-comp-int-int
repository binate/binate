VM materializes both global-address args of one instruction into the shared globalReg (lower_func.bn); the 2nd LOAD_IMM clobbers the 1st, so return &G,&H yields (&H,&H) and &G==&H compares &H==&H. Compiled backend correct. See claude-todo.
