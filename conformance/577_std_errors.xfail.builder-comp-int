errors.Error.Error() returns @[]readonly char (a 4-word managed-slice, >16 bytes) and is reached cross-mode: pkg/std/errors is compiled (builder-comp) while main is interpreted, so the call is a compiled iface method whose >16-byte return needs the VM's sret path — unimplemented (vm_exec_iface.bn aborts "vm: compiled iface method returns >16 bytes (sret path unimplemented)"). Pre-existing VM gap, unrelated to the compiler frontend; LLVM / gen2 / native pass. Tracked in claude-todo.md.
