package "pkg/store"

// Node has a managed field, so it needs a dtor / copy (NeedsDestruction).
func MakeNode(n int) @Node {
	var nd @Node = make(Node)
	nd.Tag = make_slice(int, n)
	nd.Tag[0] = n
	return nd
}

func NodeSize(nd @Node) int {
	return len(nd.Tag)
}
