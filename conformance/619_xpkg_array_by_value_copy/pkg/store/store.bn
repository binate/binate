package "pkg/store"

// Node has a managed field, so it needs a copy / dtor (NeedsDestruction).
type Node struct {
	Tag @[]int
}

func MakeNode(n int) @Node {
	var nd @Node = make(Node)
	nd.Tag = make_slice(int, n)
	nd.Tag[0] = n
	return nd
}

func NodeSize(nd @Node) int {
	return len(nd.Tag)
}
