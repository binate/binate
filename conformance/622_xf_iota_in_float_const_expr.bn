package "main"

// XFAIL (claude-todo M5, adversarial review 2026-06-06): iota inside a float
// CONST_EXPR re-lowers to 0 at the read site (CONST_EXPR stashes only the
// AST, no iota binding in scope). Expected F2 = 1.5 * 2 = 3.

const (
	F0 float64 = 1.5 * cast(float64, iota)
	F1
	F2
)

func main() {
	println(cast(int, F2))
}
