package "main"
import "pkg/builtins/testing"

// XFAIL (claude-todo C5, adversarial review 2026-06-06): a cross-package
// float const-EXPRESSION reads int 0 — gen_selector's qualified read has no
// CONST_EXPR arm and the importer never registers a float const-expr. The
// in-package equivalent works (CONST_EXPR re-lowered at the read site).
// Expected cast(int, 1.5 + 2.5) = 4.

import "pkg/cfg"

func main() {
	testing.Println(cast(int, cfg.SUM))
}
