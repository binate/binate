package "main"

// XFAIL (claude-todo M1, adversarial review 2026-06-06): a cross-package
// bool-comparison const reads int 0/false — the importer producers never
// call classifyConstBoolExpr (only classifyConstLit). The in-package
// equivalent folds correctly. Expected true.

import "pkg/cfg"

func main() {
	println(cfg.CMP)
}
