package "main"

// XFAIL (claude-todo C3, adversarial review 2026-06-06): a LOCAL const used
// as an array dimension resolves correctly in the checker but IR-gen's
// lookupConst is module-only, so resolveTypeExpr falls to parseIntLit("N")
// = garbage and allocates the wrong size. Expected len 3; observed 30.

func main() {
	const N int = 3
	var a [N]int
	println(cast(int, len(a)))
}
