package "main"

// XFAIL (claude-todo C4, adversarial review 2026-06-06): &s[i] on a
// readonly-wrapped slice (readonly @[]uint8) mis-strides: genIndexPtr uses
// arrTyp.Elem without peeling TYP_READONLY, so EmitSliceElemPtr GEPs with
// the inner-slice stride instead of the uint8 element stride. Expected 66
// ('B'); observed 0.

func main() {
	var s readonly @[]uint8 = "AB"
	var p *uint8 = &s[1]
	println(cast(int, *p))
}
