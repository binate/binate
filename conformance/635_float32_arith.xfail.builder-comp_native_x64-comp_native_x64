float32 single-precision arithmetic is implemented (aa64/x64 single-precision ops, VM BC_F32* opcodes); this x86_64-linux lane is xfailed only because it can't execute on this arm64 host (no qemu-user). The x64 codegen is identical to the passing builder-comp_native_x64_darwin lane — drop once confirmed on an x86_64/qemu host.
