package "pkg/f32"

// RetF32 returns a float32 literal — exercises the float32 scalar return ABI
// across the native↔LLVM boundary (this package is a dependency, so it is
// compiled via LLVM in the native conformance mode).
func RetF32() float32
