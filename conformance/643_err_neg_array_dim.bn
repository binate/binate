package "main"

// A negative array length has no valid layout — the checker must reject it
// cleanly rather than letting codegen emit an `alloca [-N x T]` the backend
// chokes on.

const N int = -2

func main() {
	var a [N]int
	println(len(a))
}
