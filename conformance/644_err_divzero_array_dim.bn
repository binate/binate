package "main"

// A constant array dimension that divides by zero must be reported, not
// silently resolved to a zero-length array.

const A int = 4
const B int = 0

func main() {
	var a [A/B]int
	println(len(a))
}
