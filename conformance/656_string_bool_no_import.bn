package "main"
import "pkg/builtins/testing"

// `bool.String()` resolves and links without importing pkg/builtins/lang.
// Separate per-type test (see 654).

func main() {
	var b bool = true
	testing.Println(b.String())
}
