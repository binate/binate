package "pkg/cfg"

// G is the package's struct-typed global definition (zero-init).  Nothing
// in this package references Point through a function, so the struct is
// reachable only via this global — exactly the case that forces
// collectStructTypes to scan globals.
var G Point
