NOT Defect 8 (that spurious-upcast crash is fixed; verified XPASS on all 6 other runnable modes). Blocked by the separate pre-existing int-int multi-package crash: a NULL deref in rt.MemCopy during the double-interpretation of a multi-package compile, which crashes EVERY multi-package test under int-int (e.g. the unmarked 061_cross_pkg_call / 373_cross_pkg_iface / 384_cross_pkg_iface_alias all SIGSEGV here too, with the stock compiler). Distinct from the 586/592 "package pkg/builtins/rt not found" loader bug. Un-xfail once that int-int regression is fixed — the Defect-8 fix is front-end (no OP_IFACE_UPCAST emitted) and mode-agnostic, so 665 passes int-int the moment multi-package int-int works again.
