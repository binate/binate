native OP_CAST drops the IsGlobalRef &G source (x64_dispatch.bn / aarch64_dispatch.bn fetch via bare getOperand, not emitValOperand) -> silent corruption. Native half of the &G-as-rvalue defect (Plan 3); the LLVM emitCast emitter and the VM are fixed/correct.
