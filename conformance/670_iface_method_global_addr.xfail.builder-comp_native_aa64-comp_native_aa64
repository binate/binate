native aa64 iface-method arg drops the IsGlobalRef &G source (aarch64 emitCallIfaceMethod fetches scalar args via bare getOperand). Native aa64 half (Plan 3); the LLVM emit_iface_call emitter, the VM, and native x64 (fixed 0c707e1f) are correct.
