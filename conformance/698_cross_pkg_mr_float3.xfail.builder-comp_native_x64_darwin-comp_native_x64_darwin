native x64: a 3rd/4th float in a cross-package multi-return spills to x87 (ST0/ST1) on the LLVM-compiled callee; the native x64 backend cannot emit x87 returns so it srets — divergent (open residual gap). aa64 returns it in D0..D3 (register-count rule) and passes.
