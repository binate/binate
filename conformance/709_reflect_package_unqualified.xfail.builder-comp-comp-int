Unqualified _Package() type-checks + lowers, but the bytecode VM only reaches the builtins' _Package via hand-bound externs (pkg/binate/interp/externs.bn registerPackageDescriptorExterns); main is non-builtin so the VM reports "extern not found: main._Package". Deferred VM-backend project — see claude-todo.md.
