Unqualified __Package() type-checks + lowers, but the bytecode VM only reaches the builtins' __Package via hand-bound externs (pkg/binate/interp/externs.bn registerPackageDescriptorExterns); main is non-builtin so the VM reports "extern not found: main.__Package". Deferred VM-backend project — see claude-todo.md.
