Unqualified _Package() type-checks + lowers, but the bytecode VM only reaches the four builtins' _Package via hardcoded externs (vm/extern_register_std.bn); main is non-builtin so the VM reports "extern not found: main._Package". Deferred VM-backend project — see claude-todo.md.
