package "pkg/archsel"

// GetArch returns the canonical name of the architecture this package was
// compiled for.  Its single definition is selected from per-arch impl files
// by their `#[build(...)]` constraints.
func GetArch() *[]readonly char
