package "pkg/declsel"

// Which returns a per-architecture marker.  Its single definition is
// selected from several same-named, per-arch `#[build(...)]`-gated
// definitions in one impl file.
func Which() *[]readonly char
