package "main"

import "pkg/sel"

// Verifies per-import build-constraint gating end-to-end: pkg/sel
// conditionally imports one of two per-arch helpers (and defines Pick per the
// same condition).  Only the matching import is followed.  The unit test
// loader.TestGateMergedDropsImport is the strict proof that the other import
// is dropped; this confirms the feature compiles and runs per target.
func main() {
	println(sel.Pick())
}
