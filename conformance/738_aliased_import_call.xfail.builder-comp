Aliased import cross-package call mangles the callee with the alias (bn_<alias>__Fn) instead of the package path -> undefined symbol. Pre-existing/latent (no in-repo code uses aliased imports). See explorations/claude-todo.md.
