package "main"

// A user function with an empty parameter list called with arguments is "too
// many arguments".  Only the variadic builtins (print / println / panic)
// accept extra args with an empty param list; a plain `func f()` must not.
func f() {
	println("hi")
}

func main() {
	f(1, 2)
}
