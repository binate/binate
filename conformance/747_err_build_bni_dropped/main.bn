package "main"
import "pkg/builtins/testing"

import "pkg/foo"

// pkg/foo's interface is gated out on every target (see foo.bni), so this
// import must fail to resolve regardless of arch/os.
func main() {
	testing.Println(foo.Tag)
}
