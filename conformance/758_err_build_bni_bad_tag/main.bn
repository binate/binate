package "main"
import "pkg/builtins/testing"

import "pkg/bad"

// Importing pkg/bad must fail: its .bni carries a malformed build constraint
// (unknown arch tag), which aborts the load on every target.
func main() {
	testing.Println(bad.X)
}
