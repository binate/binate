package "pkg/bad"

// "sparc" is not a recognized arch tag, so the .bni declaration gate
// (gateBni -> DeclIncluded -> evalIs) must hard-error and abort the load
// rather than silently dropping the const — a malformed constraint is a
// loud failure, on every target.
#[build(is(arch, "sparc"))]
const X int = 1
