package "main"

// A generic interface that extends its own instantiation must be
// rejected with a clean diagnostic, not crash the compiler.

interface N[T any] : N[T] { get() T }

func main() { var x *N[int]; _ = x }
