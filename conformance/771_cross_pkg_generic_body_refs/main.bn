package "main"

import "pkg/genlib"

func main() {
	var p @genlib.Pair = make(genlib.Pair)
	p.a = 3
	p.b = 4
	println(genlib.SumPair[int](p))   // 7   (non-generic struct param)
	println(genlib.Scaled[int](5))    // 50  (bare module const)
	println(genlib.ViaHelper[int](6)) // 106 (sibling free function)
	println(genlib.Outer[int](20))    // 41  (nested generic)
}
