package "main"

// A backslash that does not start a recognized escape (only \n \r
// \t \\ \' \" \0 \xHH \uHHHH are valid) is a
// compile error, rather than silently dropping the backslash.

func main() {
	println(len("\a"))
}
