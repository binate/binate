DEFERRED: same-final-segment generic imports collide at monomorphization (bb.Pick resolves to aa.Pick -> 100 100 not 100 200). Non-generic same-segment is fixed (785); generics need the monomorphization key + generic-decl stash to carry the full package path.
