package "main"
import "pkg/builtins/testing"

import "pkg/xf"

// Each file of pkg/xf aliases a DIFFERENT package as `dep`; file-scoped
// imports must keep them distinct (was: both bound to the first, printing 1,1).
func main() {
	testing.Println(xf.A())
	testing.Println(xf.B())
}
