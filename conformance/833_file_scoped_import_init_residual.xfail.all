package-init combines all files' var initializers into one synthetic body lowered under the merged import overlay, so a same-alias-different-package collision in a package-level var initializer across files mis-resolves (prints 1,1 not 1,2) — known residual of file-scoped imports (cf0d1cad); explorations/plan-file-scoped-imports.md
