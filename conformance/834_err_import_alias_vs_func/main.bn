package "main"

import p "pkg/p1"

// `p` names both the import alias and this function — must be rejected,
// not silently bound to the function (shadowing the import).
func p() int { return 5 }

func main() { println(p()) }
