package "main"

import "pkg/ca"

// pkg/ca imports pkg/cb which imports pkg/ca — a cyclic import graph.
// The loader must reject it with a clear "import cycle" diagnostic
// (was: dead detection -> confusing "unknown package"/"undefined" cascades).
func main() { println(ca.A()) }
