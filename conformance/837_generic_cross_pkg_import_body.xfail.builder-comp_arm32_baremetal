native-codegen-only: a generic body that CALLS a function from a package the consumer does not import (`dep.V()`) needs that callee declared (LLVM `declare`) as an extern in the consumer's module, but no transitive-extern declaration is emitted, so main.ll references an undeclared `@bn_pkg__dep__V` (LLVM "use of undefined value"). The qualifier now resolves to the correct package (NOT the old wrong @bn_dep__V) — the qualified-TYPE facet (field/method types in a generic body resolving against the use-site instead of the defining-file imports -> wrong package / layout corruption) is FIXED (see conformance 841). This remaining piece is the transitive-extern declaration (part c); the VM modes already PASS (the VM resolves cross-package calls via its runtime registry, no declare needed). CRITICAL; IR-gen native backends. See claude-todo.md.
