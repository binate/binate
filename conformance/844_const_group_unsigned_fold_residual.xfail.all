grouped const Q = U/D (unsigned operands >= 2^63) folds signed on both checker and IR-gen -> -4 not 4; signedness-aware fold is gated to single consts (InConstGroup), genConstGroup not yet wired to the ExprType stamp
