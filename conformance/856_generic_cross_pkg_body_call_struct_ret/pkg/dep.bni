package "pkg/dep"

type Pair struct { a int; b int }

// Returns a STRUCT by value (the harder case: a guessed/fallback signature
// would treat the result as a scalar and miscompile the aggregate).
func Make() Pair
