package "main"
import "pkg/builtins/testing"

import "pkg/genlib"

// main does NOT import pkg/dep.  genlib.Sum[int]'s body calls dep.Two()
// (multi-return); the consumer must declare it with the real signature.
// Want: 42.
func main() {
	testing.Println(genlib.Sum[int](0))
}
