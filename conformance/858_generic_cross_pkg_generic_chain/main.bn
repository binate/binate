package "main"
import "pkg/builtins/testing"

import "pkg/genlib"

// main imports ONLY genlib.  genlib.Sum[int] -> mid.F[int] -> deep.G().
// Tests whether the transitive-extern registration reaches deep (two levels
// below the consumer's direct import).  Want: 42.
func main() {
	testing.Println(genlib.Sum[int](0))
}
