package "pkg/genlib"

import "pkg/mid"

// generic body instantiates ANOTHER package's generic (mid.F), which itself
// calls a yet-deeper package (deep) the consumer never imports.
func Sum[T any](x T) int { return mid.F[T](x) }
