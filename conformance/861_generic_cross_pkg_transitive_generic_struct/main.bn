package "main"

import "pkg/genlib"

// main imports ONLY genlib.  genlib.Wrap[int]'s body uses mid.Box[int] (mid
// transitive); the consumer must register mid.Box's generic decl to
// monomorphize.  Want: 42.
func main() {
	println(genlib.Wrap[int](0))
}
