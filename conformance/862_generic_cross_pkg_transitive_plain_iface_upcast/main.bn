package "main"

import "pkg/genlib"

// main imports ONLY genlib.  genlib.Run[int]'s body upcasts to *mid.Box (a
// plain interface in the transitive package mid).  Want: 7.
func main() {
	println(genlib.Run[int](0))
}
