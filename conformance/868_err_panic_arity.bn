package "main"

// panic takes exactly one (string) message argument — the old variadic form
// (multiple args, value formatting) is gone.  Two args must be rejected.
func main() {
	panic("first", "second")
}
