VM mis-handles a func-value call whose function returns a managed slice (@[]char): yields empty output. Pre-existing VM limitation, independent of the composite-literal element lowering (which is correct in every backend — cf. 875). See explorations/claude-todo.md.
