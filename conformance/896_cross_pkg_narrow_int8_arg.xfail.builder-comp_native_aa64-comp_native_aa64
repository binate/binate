int8/int16 fixed narrow stack args are rounded to 8 bytes by the native aa64 caller (StackArgNarrow4 only handles SizeOf==4), while Apple-AArch64 LLVM packs them at 1/2-byte stride -> cross-boundary offset miscompile for >8 int8/int16 args. The int8/int16 follow-up to commit 3 (extend StackArgNarrow4 + Strb/Strh stores). claude-todo #121 / plan-fp-arg-stack-overflow.
