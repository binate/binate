a subslice of a distinct named managed-slice (`type Buf @[]int`) miscompiles on the native aarch64 backend (the named-managed-slice index/len/slice gap): produces wrong output or crashes (works on LLVM, VM, gen1, gen2). The IR-gen backing-pin fix (962fdb19) makes the header correct, but the native aarch64 backend still mishandles the named managed-slice. Same gap as 033/036. claude-todo: native-aa64-named-managed-slice
