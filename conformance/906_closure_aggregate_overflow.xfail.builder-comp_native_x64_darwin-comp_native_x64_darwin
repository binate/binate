native x64's aggregate-return capturing-closure shim still raises a.SetError on the stack-spill shape (captures + user args exceed the GP arg regs, or incoming user words exceed the budget) — the x64 analogue of the aarch64 stack-spill path added for GAP D (plan-fp-arg-stack-overflow). Tracked as a follow-up.
