native codegen emits no output (empty stdout) for a NAMED managed-slice struct field — a native-backend defect separate from the IR-gen ResolveAlias-vs-peelTransparent mis-route (now fixed: LLVM/IR-gen passes). See claude-todo.md (native named-managed-slice struct-field).
