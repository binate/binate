package "main"

// The zero-padded string -> array init only goes one way: a string literal may
// be SHORTER than the target (padded), but an OVER-LONG literal does not fit a
// smaller fixed array and is rejected (the M >= N boundary of stringLitFitsArray).
func main() {
	var b [3]readonly char = "alpha"
	_ = b
}
