cross-package method value on a CALL-result receiver (NewBox(77).get) is not qualified — methodValueRecvIRType has no EXPR_CALL case, falls back to the importer package and fails to link. claude-todo.md (LANE 2 method-value residual).
