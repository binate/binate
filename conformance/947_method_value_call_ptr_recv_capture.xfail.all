a method value with a POINTER receiver on a VALUE-returning call/temp (`mk().get`) stores the Box value into the closure *Box slot as a wild pointer -> SIGSEGV at call. genCapturedRecv T->*T handles only EXPR_IDENT; a call-result temp needs materialize+address (Go semantics). Pre-existing (local + cross-package). claude-todo.md.
