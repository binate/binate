package "main"

import "pkg/gen"

func main() {
	var h @gen.Holder[int] = gen.MakeMgd(5)
	println(h.get())          // 5

	var t gen.Thing = h.boxed()
	println(t.a)              // 5
	println(t.b)              // 105
}
