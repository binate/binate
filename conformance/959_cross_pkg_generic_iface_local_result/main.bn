package "main"
import "pkg/builtins/testing"

import "pkg/gen"

func main() {
	var h @gen.Holder[int] = gen.MakeMgd(5)
	testing.Println(h.get())          // 5

	var t gen.Thing = h.boxed()
	testing.Println(t.a)              // 5
	testing.Println(t.b)              // 105
}
