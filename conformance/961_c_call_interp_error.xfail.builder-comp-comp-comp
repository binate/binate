__c_call works when compiled (native FFI), so this NEGATIVE test — which asserts the bytecode VM's "cannot interpret __c_call" error — produces normal output here instead of erroring, and is meaningful only in the VM modes.  Positive (compiled) coverage is 498.
