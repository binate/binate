The thunk-dispatch change (98219ab3e) routes EVERY func-value call — including this test's bytecode->bytecode call — through the vtable.call trampoline / rt._call_shim_* boundary, which is scalar-only and caps at 7 user words, so it cannot carry this test's cross-mode arg/return shape (FP-register floats, natural-size narrow args, by-retbuf aggregates, and/or >7 spilled words) — the VM dispatch drops or mis-marshals them. Pre-98219ab3e a bytecode->bytecode func-value call dispatched inline (execFunc, uniform int-slots) and this passed. To be un-xfail'd by the cross-mode any-shape func-value dispatch fix (claude-todo); the compiled native/LLVM lanes (per-signature shim spill) still pass.
