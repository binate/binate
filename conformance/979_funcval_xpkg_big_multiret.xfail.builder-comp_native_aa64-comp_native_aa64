func-value SMALL/BIG cross-pkg multi-return: the LLVM-emitted shim uses the retbuf-as-first-arg convention for ANY multi-return (IsAggregateReturn = len(results)>1), but the native emitCallFuncValue caller collects a small (<=NumGpRetRegs-word) multi-return from return registers to match the NATIVE same-module shim — the two shim families disagree and the caller can't tell which it reaches. See claude-todo.md 'func-value SMALL multi-return: native-shim vs LLVM-shim ABI CONFLICT'.
