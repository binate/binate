aa64 native backend is internally inconsistent for a STRADDLING GP aggregate arg through an iface: caller vs callee disagree, spilled p4 reads 0 (650321 vs 654321). Part of the native<->LLVM straddling-aggregate ABI bug (LLVM splits [N x i64], native+clang all-or-nothing) — see claude-todo.md.
