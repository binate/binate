package "pkg/gholder"

// Concrete helper so the package compiles on its own -- Holder[T]'s bodies live
// in the .bni for the cross-package consumer to monomorphize.
func Sentinel() int {
	return 1
}
