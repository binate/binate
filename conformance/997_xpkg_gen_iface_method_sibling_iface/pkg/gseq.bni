package "pkg/gseq"

// The Iterable/Iterator shape: a generic interface (Iterable[T]) whose method
// returns a SIBLING generic INTERFACE (@Iterator[T]) forwarding the type param.

interface Iterator[T any] {
	Next() (T, bool)
}

interface Iterable[T any] {
	AsIterator() @Iterator[T]
}
