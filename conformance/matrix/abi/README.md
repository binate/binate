# ABI matrix

The **aggregate & multi-return passing** (Class 4) member of the conformance
matrix family (`../README.md`). Coordinate-addressed:

    conformance/matrix/abi/<category>/<...>.bn   (+ .expected)

Generated by `conformance/gen-abi-matrix.py`.

## Categories & assertion

- **`multi-return/<type>/<arity>`** — a function returns an N-tuple; the caller
  binds all components (`a, b, c := f()`) and asserts each.
- **`struct-return/<shape>`** — a function returns a struct by value; the caller
  reads every field.
- **`struct-param/<shape>`** — a struct is passed by value; the callee reads
  every field.

Assertion: **every field's value survives the call boundary** (a mis-packed
tuple/aggregate reads a wrong value). Shapes are chosen to hit the ABI edges —
non-8-multiple sizes (`three-u32` = 12B), internal padding (`u16-int`), sub-word
packing (`five-u8` = 5B), and arities past the in-register limit.

## Current state

LLVM and the VM pass every cell. The native backends mis-pack several aggregates
(CONFIRMED, xfailed + filed `claude-todo.md` / §3.9):

- **aa64** — struct param+return of `three-u32` (non-8-multiple) and `five-u8`
  (sub-word-packed): the trailing field is corrupted.
- **x64** — sub-word (`uint16`) multi-return at arity ≥ 3, and struct **param**
  of `three-u32` / `five-u8` (struct-*return* works on x64).

The x64 multi-return n=2-cap from the audit (§3.1) is **FIXED** — all-int
multi-return up to arity 5 passes everywhere.

## Planned

The **call-shape** axis (iface-method / func-value / handle / `__c_call`) — to
catch the byval/sret-threshold disagreements (§3.9); plus more aggregate shapes
(nested, larger, managed-field).
