package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — struct-param via function-value call, shape=three-u32.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md.

type S struct {
	f0 uint32
	f1 uint32
	f2 uint32
}

func sink(s S) {
	testing.Println(cast(int, s.f0))
	testing.Println(cast(int, s.f1))
	testing.Println(cast(int, s.f2))
}

func main() {
	var s S
	s.f0 = 100
	s.f1 = 200
	s.f2 = 300
	var f @func(S) = sink
	f(s)
}
