package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface dispatch (= assign form), type=f64, arity=3.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md.

type Impl struct {
	n int
}

func (im *Impl) mr() (float64, float64, float64) {
	return 100.0, 200.0, 300.0
}

interface Multi {
	mr() (float64, float64, float64)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	var a0 float64
	var a1 float64
	var a2 float64
	a0, a1, a2 = m.mr()
	testing.Println(cast(int, a0))
	testing.Println(cast(int, a1))
	testing.Println(cast(int, a2))
}
