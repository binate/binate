package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface dispatch (= assign form), type=int, arity=2.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type Impl struct {
	n int
}

func (im *Impl) mr() (int, int) {
	return 10, 20
}

interface Multi {
	mr() (int, int)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	var a0 int
	var a1 int
	a0, a1 = m.mr()
	println(a0)
	println(a1)
}
