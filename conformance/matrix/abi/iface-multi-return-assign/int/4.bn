package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface dispatch (= assign form), type=int, arity=4.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type Impl struct {
	n int
}

func (im *Impl) mr() (int, int, int, int) {
	return 10, 20, 30, 40
}

interface Multi {
	mr() (int, int, int, int)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	var a0 int
	var a1 int
	var a2 int
	var a3 int
	a0, a1, a2, a3 = m.mr()
	println(a0)
	println(a1)
	println(a2)
	println(a3)
}
