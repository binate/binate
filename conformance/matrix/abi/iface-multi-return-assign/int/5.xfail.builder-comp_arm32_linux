iface dispatch multi-return: native tuple-packing not yet implemented (Plan-3 native multi-return packing). The front-end SEAM that dropped the result type is FIXED (6c39d460) and LLVM is green; only the native backends remain.
