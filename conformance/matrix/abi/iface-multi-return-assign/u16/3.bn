package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface dispatch (= assign form), type=u16, arity=3.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type Impl struct {
	n int
}

func (im *Impl) mr() (uint16, uint16, uint16) {
	return 100, 200, 300
}

interface Multi {
	mr() (uint16, uint16, uint16)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	var a0 uint16
	var a1 uint16
	var a2 uint16
	a0, a1, a2 = m.mr()
	println(cast(int, a0))
	println(cast(int, a1))
	println(cast(int, a2))
}
