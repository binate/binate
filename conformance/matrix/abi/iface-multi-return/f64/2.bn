package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface-method dispatch, type=f64, arity=2.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type Impl struct {
	n int
}

func (im *Impl) mr() (float64, float64) {
	return 100.0, 200.0
}

interface Multi {
	mr() (float64, float64)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	a0, a1 := m.mr()
	println(cast(int, a0))
	println(cast(int, a1))
}
