package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface-method dispatch, type=f64, arity=4.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md.

type Impl struct {
	n int
}

func (im *Impl) mr() (float64, float64, float64, float64) {
	return 100.0, 200.0, 300.0, 400.0
}

interface Multi {
	mr() (float64, float64, float64, float64)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	a0, a1, a2, a3 := m.mr()
	println(cast(int, a0))
	println(cast(int, a1))
	println(cast(int, a2))
	println(cast(int, a3))
}
