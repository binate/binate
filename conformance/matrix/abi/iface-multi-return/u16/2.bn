package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface-method dispatch, type=u16, arity=2.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md.

type Impl struct {
	n int
}

func (im *Impl) mr() (uint16, uint16) {
	return 100, 200
}

interface Multi {
	mr() (uint16, uint16)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	a0, a1 := m.mr()
	testing.Println(cast(int, a0))
	testing.Println(cast(int, a1))
}
