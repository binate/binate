package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — multi-return via interface-method dispatch, type=u16, arity=5.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type Impl struct {
	n int
}

func (im *Impl) mr() (uint16, uint16, uint16, uint16, uint16) {
	return 100, 200, 300, 400, 500
}

interface Multi {
	mr() (uint16, uint16, uint16, uint16, uint16)
}

impl *Impl : Multi

func main() {
	var m @Multi = make(Impl)
	a0, a1, a2, a3, a4 := m.mr()
	println(cast(int, a0))
	println(cast(int, a1))
	println(cast(int, a2))
	println(cast(int, a3))
	println(cast(int, a4))
}
