package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — struct-param via interface-method dispatch, shape=three-u32.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type S struct {
	f0 uint32
	f1 uint32
	f2 uint32
}

type Impl struct {
	n int
}

func (im *Impl) sink(s S) {
	println(cast(int, s.f0))
	println(cast(int, s.f1))
	println(cast(int, s.f2))
}

interface Sink {
	sink(s S)
}

impl *Impl : Sink

func main() {
	var s S
	s.f0 = 100
	s.f1 = 200
	s.f2 = 300
	var snk @Sink = make(Impl)
	snk.sink(s)
}
