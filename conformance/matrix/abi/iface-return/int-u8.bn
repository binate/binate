package "main"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — struct-return via interface-method dispatch, shape=int-u8.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md and
// plan-code-red.md §3.9.

type S struct {
	f0 int
	f1 uint8
}

type Impl struct {
	n int
}

func (im *Impl) makeS() S {
	var s S
	s.f0 = 1000
	s.f1 = 9
	return s
}

interface Maker {
	makeS() S
}

impl *Impl : Maker

func main() {
	var mk @Maker = make(Impl)
	var s S = mk.makeS()
	println(s.f0)
	println(cast(int, s.f1))
}
