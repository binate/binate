package "main"

import "pkg/builtins/rt"

// GENERATED by conformance/gen-abi-matrix.py — do not edit by hand.
// ABI matrix cell — managed multi-return via funcval (refcount balance), arity=2.
// Every field's value must survive the call boundary; a mis-packed
// tuple/aggregate reads a wrong value here. See ../../README.md.

type Node struct {
	Val int
}

func mr(x0 @Node, x1 @Node) (@Node, @Node) {
	return x0, x1
}

func thread(x0 @Node, x1 @Node) {
	var f @func(@Node, @Node) (@Node, @Node) = mr
	a0, a1 := f(x0, x1)
	println(a0.Val)
	println(a1.Val)
}

func main() {
	var n0 @Node = make(Node)
	n0.Val = 10
	var n1 @Node = make(Node)
	n1.Val = 20
	thread(n0, n1)
	println(rt.Refcount(bit_cast(*uint8, n0)))
	println(rt.Refcount(bit_cast(*uint8, n1)))
}
