package "main"

// GENERATED by conformance/gen-aggregate-matrix.py — do not edit by hand.
// Aggregate value-movement cell — form=field, kind=array-float.
// Every lane must read back its value after the movement; a dropped or
// garbled aggregate/global prints 0 here. See ../../README.md.

type W struct {
	agg [3]float64
}

func main() {
	var w W
	w.agg = [3]float64{1.5, 2.5, 3.5}
	println(cast(int, w.agg[0] == 1.5))
	println(cast(int, w.agg[1] == 2.5))
	println(cast(int, w.agg[2] == 3.5))
}
