package "main"

// GENERATED by conformance/gen-aggregate-matrix.py — do not edit by hand.
// Aggregate value-movement cell — form=field, kind=array-int.
// Every lane must read back its value after the movement; a dropped or
// garbled aggregate/global prints 0 here. See ../../README.md.

type W struct {
	agg [3]int
}

func main() {
	var w W
	w.agg = [3]int{11, 22, 33}
	println(cast(int, w.agg[0] == 11))
	println(cast(int, w.agg[1] == 22))
	println(cast(int, w.agg[2] == 33))
}
