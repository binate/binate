package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-aggregate-matrix.py — do not edit by hand.
// Aggregate value-movement cell — form=field, kind=struct-float.
// Every lane must read back its value after the movement; a dropped or
// garbled aggregate/global prints 0 here. See ../../README.md.

type S struct {
	f0 float64
	f1 float64
	f2 float64
}

type W struct {
	agg S
}

func main() {
	var w W
	w.agg = S{f0: 1.5, f1: 2.5, f2: 3.5}
	testing.Println(cast(int, w.agg.f0 == 1.5))
	testing.Println(cast(int, w.agg.f1 == 2.5))
	testing.Println(cast(int, w.agg.f2 == 3.5))
}
