package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-const-matrix.py — do not edit by hand.
// const matrix cell — read-form=const-group, type=uint64, value=max (0xFFFFFFFFFFFFFFFF).
// The constant must materialize to the exact bit pattern of its type; printed
// as 16-bit little-endian lanes (target-stable). See ../../README.md.

const (
	C0 uint64 = 0xFFFFFFFFFFFFFFFF
	C1 uint64 = 0
)

func main() {
	var x uint64 = C0
	testing.Println(cast(int, (x) & 0xFFFF))
	testing.Println(cast(int, ((x) >> 16) & 0xFFFF))
	testing.Println(cast(int, ((x) >> 32) & 0xFFFF))
	testing.Println(cast(int, ((x) >> 48) & 0xFFFF))
}
