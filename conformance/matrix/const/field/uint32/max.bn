package "main"

// GENERATED by conformance/gen-const-matrix.py — do not edit by hand.
// const matrix cell — read-form=field, type=uint32, value=max (0xFFFFFFFF).
// The constant must materialize to the exact bit pattern of its type; printed
// as 16-bit little-endian lanes (target-stable). See ../../README.md.

type S struct {
	f uint32
}

func main() {
	var s S = S{f: 0xFFFFFFFF}
	println(cast(int, (cast(uint64, s.f)) & 0xFFFF))
	println(cast(int, ((cast(uint64, s.f)) >> 16) & 0xFFFF))
}
