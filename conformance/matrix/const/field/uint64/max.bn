package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-const-matrix.py — do not edit by hand.
// const matrix cell — read-form=field, type=uint64, value=max (0xFFFFFFFFFFFFFFFF).
// The constant must materialize to the exact bit pattern of its type; printed
// as 16-bit little-endian lanes (target-stable). See ../../README.md.

type S struct {
	f uint64
}

func main() {
	var s S = S{f: 0xFFFFFFFFFFFFFFFF}
	testing.Println(cast(int, (s.f) & 0xFFFF))
	testing.Println(cast(int, ((s.f) >> 16) & 0xFFFF))
	testing.Println(cast(int, ((s.f) >> 32) & 0xFFFF))
	testing.Println(cast(int, ((s.f) >> 48) & 0xFFFF))
}
