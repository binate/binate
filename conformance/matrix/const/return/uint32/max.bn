package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-const-matrix.py — do not edit by hand.
// const matrix cell — read-form=return, type=uint32, value=max (0xFFFFFFFF).
// The constant must materialize to the exact bit pattern of its type; printed
// as 16-bit little-endian lanes (target-stable). See ../../README.md.

func g() uint32 {
	return 0xFFFFFFFF
}

func main() {
	var x uint32 = g()
	testing.Println(cast(int, (cast(uint64, x)) & 0xFFFF))
	testing.Println(cast(int, ((cast(uint64, x)) >> 16) & 0xFFFF))
}
