package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — constraint/dispatch.
// A generic function body that calls a method on its type param's CONSTRAINT interface (`func f[T Show](x T) int { return x.show() }`) dispatches correctly in the instantiation (mirrors 434).
// See ../../README.md.

import "pkg/builtins/rt"

interface Show {
	show() int
}

type Widget struct {
	v int
}

func (w *Widget) show() int {
	return w.v
}

impl *Widget : Show

func showIt[T Show](x T) int {
	return x.show()
}

func main() {
	var w Widget
	w.v = 42
	var p *Widget = &w
	testing.Println(showIt[*Widget](p))
}
