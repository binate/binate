package "main"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — distinct/array-len.
// Two generic instantiations differing ONLY within a structured type arg (array LENGTH, func SIGNATURE) are DISTINCT types; assigning one to the other is a compile error (42b3bc83).
// See ../../README.md.

import "pkg/builtins/rt"

type Box[T any] struct {
	fn T
}

func main() {
	var b3 @Box[[3]int] = make(Box[[3]int])
	var b5 @Box[[5]int] = make(Box[[5]int])
	b3 = b5
}
