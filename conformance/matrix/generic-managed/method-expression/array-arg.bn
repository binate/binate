package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — method-expression/array-arg.
// A method EXPRESSION off a generic instantiation (`Box[int].Get`, receiver as Params[0] — the unbound form of the method value) links + runs: the func value binds the mangled instantiation method, the SAME symbol the bound form (`bp.Get`) and direct calls use.
// See ../../README.md.

import "pkg/builtins/rt"

type Box[T any] struct {
	val T
	tag int
}

func (b *Box[T]) Tag() int {
	return b.tag
}

func main() {
	var bx Box[[4]int]
	bx.tag = 42
	var f *func(*Box[[4]int]) int = Box[[4]int].Tag
	testing.Println(f(&bx))
}
