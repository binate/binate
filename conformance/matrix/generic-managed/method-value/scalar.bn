package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — method-value/scalar.
// A method VALUE taken off a generic instantiation (or a generic-call result) links + runs: the captured-receiver closure struct is named by the MANGLED instantiation name on every name-mangling backend (2d48f348/fedbd0c5).
// See ../../README.md.

import "pkg/builtins/rt"

type Box[T any] struct {
	val T
}

func (b *Box[T]) Get() T {
	return b.val
}

func main() {
	var bx Box[int]
	bx.val = 42
	var bp *Box[int] = &bx
	var f *func() int = bp.Get
	testing.Println(f())
}
