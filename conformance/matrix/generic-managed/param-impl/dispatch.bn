package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — param-impl/dispatch.
// A parameterized-receiver impl (`impl @Box[T] : Getter[T]`) lets a concrete `@Box[int]` box into a `@Getter[int]` interface value and dispatch through the per-instantiation vtable (gen.impl.generic-recv; mirrors 447).
// See ../../README.md.

import "pkg/builtins/rt"

interface Getter[T any] {
	Get() T
}

type Box[T any] struct {
	val T
}

func (b @Box[T]) Get() T {
	return b.val
}

impl @Box[T] : Getter[T]

func main() {
	var b @Box[int] = make(Box[int])
	b.val = 42
	var g @Getter[int] = b
	testing.Println(g.Get())
}
