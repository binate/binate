package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-generic-managed-matrix.py — do not edit by hand.
// generic-managed matrix cell — xpkg/func-value/balance.
// A managed element held in a generic container gains exactly one ref and is released when the container drops (== before+1, then == before) — no leak, no UAF.
// See ../../README.md.

import "pkg/builtins/rt"

import "pkg/gh"

func main() {
	var nn int = 42
	var src @func() int = func() int { return nn }
	var hp *int = bit_cast(*int, &src)
	var po *uint8 = bit_cast(*uint8, hp[1])
	var h @gh.Holder[@func() int] = gh.New[@func() int]()
	var before int = rt.Refcount(po)
	gh.Put[@func() int](h, src)
	testing.Println(cast(int, rt.Refcount(po) == before + 1))
	testing.Println(gh.At[@func() int](h, 0)())
	var drp @gh.Holder[@func() int] = gh.New[@func() int]()
	h = drp
	testing.Println(cast(int, rt.Refcount(po) == before))
}
