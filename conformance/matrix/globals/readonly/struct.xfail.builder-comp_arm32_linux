readonly field-read returns literal-0 — gen_selector falls through every Kind-test for a readonly value (claude-todo readonly CRITICAL Symptom 1; plan-cr2-1 Defect 1). Shared IR-gen bug -> all backends.
