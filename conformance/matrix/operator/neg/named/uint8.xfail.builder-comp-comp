unary minus on a NAMED sub-word int emits invalid LLVM (sub i64 0, %i8) — gen_expr MINUS arm gates on TYP_INT, never peels TYP_NAMED (plan-cr2-1 Round-2 finding 8; the ~ arm peels it, so bitnot/named passes). LLVM-only: VM + native tolerate the mis-typed IR.
