package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-readonly-matrix.py — do not edit by hand.
// readonly matrix cell — alias/method-receiver.
// A readonly view must observe the same value / dispatch as the plain value;
// `readonly` is a transparent wrapper every Kind-dispatch must peel.
// See ../README.md.

type Box struct {
	v int
}

func (b *Box) getV() int {
	return b.v
}

type AB = @Box

func mkBox(n int) @Box {
	var b @Box = make(Box)
	b.v = n
	return b
}

func main() {
	var x AB = mkBox(7)
	testing.Println(x.getV())
}
