package "main"

// GENERATED by conformance/gen-readonly-matrix.py — do not edit by hand.
// readonly matrix cell — method/iface.
// A readonly view must observe the same value / dispatch as the plain value;
// `readonly` is a transparent wrapper every Kind-dispatch must peel.
// See ../README.md.

interface Getter {
	get() int
}

type Impl struct {
	v int
}

func (im *Impl) get() int {
	return im.v
}

impl *Impl : Getter

func main() {
	var im @Impl = make(Impl)
	im.v = 7
	var r @Getter = im
	var rr readonly @Getter = r
	println(rr.get())
}
