readonly field-read returns literal-0 — gen_selector falls through every Kind-test for a readonly value (claude-todo readonly CRITICAL Symptom 1; plan-cr2-1 Defect 1). Shared IR-gen -> all backends. (arm32 compiles via the LLVM/clang path, and on ILP32 Big is 12 B (<= 16) so the >16-byte byval convention isn't involved here at all.)
