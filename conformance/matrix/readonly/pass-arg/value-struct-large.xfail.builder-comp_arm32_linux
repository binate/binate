native common.IsAggregateTyp doesn't peel readonly -> a >16-byte readonly aggregate param is passed by value (the native half of R2-1; the LLVM codegen + IR-gen halves are fixed). Plan 3 owns the native classifier.
