package "main"

import "pkg/builtins/testing"

// GENERATED by conformance/gen-matrix.py — do not edit by hand.
// Matrix cell — form=array-lit, shape=elem, type=func-value.
// Refcount-balance with a MORTAL source: copying the value into the target
// raises its observable's refcount by one; dropping the alias restores the
// baseline. See conformance/matrix/README.md.

import "pkg/builtins/rt"

func main() {
	var src_n int = 5
	var src @func() int = func() int { return src_n }
	var src_hp *int = bit_cast(*int, &src)
	var src_po *uint8 = bit_cast(*uint8, src_hp[1])
	testing.Println(rt.Refcount(src_po))
	var oth_z int = 0
	var oth @func() int = func() int { return oth_z }
	var arr [2]@func() int = [2]@func() int{src, oth}
	testing.Println(rt.Refcount(src_po))
	testing.Println(arr[0]())
	var drp_z int = 0
	var drp @func() int = func() int { return drp_z }
	arr[0] = drp
	testing.Println(rt.Refcount(src_po))
}
