VM float32 -> unsigned int uses the SIGNED conversion (BC_F32TOSI): cast(uint64, <float32 >= 2^63>) saturates to INT64_MAX instead of the in-range unsigned value (tuple 16, the 2^63 float32 round-trip). Distinct from the now-fixed int->float32 bug. claude-todo: vm-float32-to-unsigned
