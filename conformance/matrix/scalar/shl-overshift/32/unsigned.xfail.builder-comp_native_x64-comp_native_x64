shift by a count >= the operand width is masked (count mod width) instead of the spec's defined 0 (<< / logical >>) / sign-fill (arithmetic >>) -- CONFIRMED on every backend (LLVM/VM/native). claude-todo CRITICAL (shift-by->=-width); fix in progress, un-xfail when it lands.
