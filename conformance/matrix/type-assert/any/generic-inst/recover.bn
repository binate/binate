package "main"
import "pkg/builtins/testing"

// GENERATED by conformance/gen-type-assert-matrix.py — do not edit by hand.
// type-assert matrix cell — any/generic-inst/recover.
// A generic-INSTANTIATION struct (`Box[int]`) value-recovers from a `*any` box, and is a DISTINCT type from another instantiation (`Box[bool]`) — a comma-ok on the wrong instantiation misses; the RTTI type identity keys on the monomorphized type, across every mode.
// See ../../README.md.

type Box[T any] struct {
	v T
}

func main() {
	var b Box[int]
	b.v = 42
	var a *any = &b
	var e Box[int] = a.(Box[int])
	if e.v == 42 { testing.Println(1) } else { testing.Println(-1) }
	r, ok := a.(Box[int])
	if ok && r.v == 42 { testing.Println(2) } else { testing.Println(-2) }
	_, ok2 := a.(Box[bool])
	if ok2 { testing.Println(-3) } else { testing.Println(3) }
}
