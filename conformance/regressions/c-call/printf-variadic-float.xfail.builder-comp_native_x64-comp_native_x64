variadic float __c_call arg passed wrong on the native backends: x64-SysV must set AL=number-of-vector-args (double in XMM0), darwin-arm64 stacks every vararg as an 8-byte slot; the native backends do neither, so printf reads 0 instead of the double. CONFIRMED — claude-todo (variadic-float native ABI; §3.9).
