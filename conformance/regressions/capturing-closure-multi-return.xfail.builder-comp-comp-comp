capturing-closure (@func with env) multi-return call reads results from the wrong location -> garbage on LLVM (managed component -> crash); VM + native correct. LLVM-codegen-only. CONFIRMED 2026-06-09.
