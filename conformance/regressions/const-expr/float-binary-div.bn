package "main"
import "pkg/builtins/testing"

// Known bug (claude-todo const-expr non-int dropped): binary float division in a const. 7.0 / 2.0 = 3.5;
// cast(int, 3.5) = 3 if the fold worked. Dropped via the same path
// (classifyConstLit miss -> integer evalConstExpr -> (0,false) ->
// const not appended).

const HALF7 float64 = 7.0 / 2.0

func main() {
	testing.Println(cast(int, HALF7))
}
