non-integer const-expr (binary-float / bool-comparison / const-as-array-dim) is silently dropped by IR-gen's int-only evalConstExpr and read back as int 0 (all backends; front-end) - claude-todo (const-expr non-int dropped).
