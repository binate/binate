VM single-return copy-back has no func-value (16-byte aggregate) arm, so a func value returned-then-passed-directly carries a dangling address into the reclaimed callee frame -> nil vtable at dispatch. VM-only; compiled is correct. claude-todo / plan-cr-p2-3 defect 3.8.
