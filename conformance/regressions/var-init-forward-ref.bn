package "main"
import "pkg/builtins/testing"

// `b` forward-references `a`; dependency order initializes `a` first, so
// `b` reads 41 (not the zero value a plain source-order init would give).
var b int = a + 1
var a int = 41

func main() {
	testing.Println(b)
}
