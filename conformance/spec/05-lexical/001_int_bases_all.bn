package "main"

// lex.literal.int.bases: integer literals are written in one of four bases
// (decimal, hex 0x, octal 0o, binary 0b) and all denote the same int values.
func main() {
	if 0xFF == 255 && 0o17 == 15 && 0b101 == 5 && 255 == 255 { println("ok") } else { println("FAIL") }
}
