package "main"

// lex.literal.int.bases: digit-range boundaries per base -- hex admits 0-9/a-f,
// octal 0-7, binary 0-1. Exercises the top valid digit of each base.
func main() {
	if 0xf == 15 && 0xF == 15 && 0o7 == 7 && 0b1 == 1 { println("ok") } else { println("FAIL") }
}
