package "main"

// lex.literal.int.bases / lex.error.illegal: a base prefix 0x with no following
// hex digit is not a valid hex literal -- the lexer emits an illegal token, so
// the parser cannot start an expression from it.
func main() {
	var x int = 0x
	_ = x
}
