package "main"

// lex.literal.int.bases / lex.error.illegal: a base prefix 0o with no following
// octal digit is not a valid octal literal -- illegal token, cannot begin an
// expression.
func main() {
	var x int = 0o
	_ = x
}
