package "main"

// lex.literal.int.bases / lex.error.illegal: a base prefix 0b with no following
// binary digit is not a valid binary literal -- illegal token, cannot begin an
// expression.
func main() {
	var x int = 0b
	_ = x
}
