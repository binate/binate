package "main"

// lex.literal.int.zero: a single 0 is the integer zero, and the zero value is
// expressible in every base (0, 0x0, 0o0, 0b0 all denote 0).
func main() {
	if 0 == 0 && 0x0 == 0 && 0o0 == 0 && 0b0 == 0 { println("ok") } else { println("FAIL") }
}
