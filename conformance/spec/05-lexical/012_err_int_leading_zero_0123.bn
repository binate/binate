package "main"

// lex.literal.int.leading-zero: a multi-digit numeral beginning with 0 and not
// using a base prefix (0123) is a lexical error, not a decimal literal --
// decimal_lit admits a leading 0 only as the single digit 0.
func main() {
	var x int = 0123
	_ = x
}
