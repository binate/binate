package "main"

// lex.literal.int.leading-zero: even 00 (two zeros) is ill-formed -- decimal_lit
// admits a leading 0 only as the single digit 0, so any second digit makes it
// an illegal token. This pins the boundary just past the valid single 0.
func main() {
	var x int = 00
	_ = x
}
