package "main"

// lex.literal.float.forms (Constraint): the exponent's `digit { digit }` is
// mandatory -- an `e`/`E` with no following digit is not a valid float form.
// The lexer emits an illegal token, so the parser cannot start an expression.
func main() {
    var x float64 = 1.5e
    _ = x
}
