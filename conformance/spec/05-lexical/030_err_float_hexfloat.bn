package "main"

// lex.literal.float.no-hexfloat (Constraint): there are no hexadecimal
// floating-point literals. `0x1.5p4` is not a float; the `0x1` integer prefix
// followed by `.5p4` does not form a token sequence the parser accepts.
func main() {
    var x float64 = 0x1.5p4
    _ = x
}
