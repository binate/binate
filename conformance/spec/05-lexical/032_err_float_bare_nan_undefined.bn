package "main"

// lex.literal.float.no-hexfloat (Constraint): `NaN` is not a float literal --
// bare, it is an ordinary identifier with no universe binding, so using it as
// a value is rejected as an undefined name (not parsed as a special value).
func main() {
    var x float64 = NaN
    _ = x
}
