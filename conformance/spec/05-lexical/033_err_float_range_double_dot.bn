package "main"

// lex.literal.float.range-carveout (Constraint): two dots in a row are never
// part of a float -- `1..2` tokenizes as `1` `.` `.` `2` (the `..` is reserved
// for future range syntax), so it is not a valid expression and is rejected.
func main() {
    var x int = 1..2
    _ = x
}
