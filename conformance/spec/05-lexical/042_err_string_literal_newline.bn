package "main"

// lex.literal.string.token: a literal newline is not permitted inside a string
// literal and ends it, so a string split across a source line does not tokenize
// as a single string -- the broken source is rejected at compile time.
func main() {
	var s @[]readonly char = "ab
cd"
	_ = s
}
