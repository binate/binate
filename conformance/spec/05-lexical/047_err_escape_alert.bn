package "main"

// lex.escape.unsupported: \a (alert) is NOT a recognized escape; the spec lists
// it as unsupported, and the implementation rejects it at compile time rather
// than silently dropping the backslash.
func main() {
	var c char = '\a'
	_ = c
}
