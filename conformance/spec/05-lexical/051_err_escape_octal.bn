package "main"

// lex.escape.unsupported: octal escapes such as \101 are NOT supported. The
// scanner reads the backslash plus the first digit as the escape \1, which is
// unrecognized, and rejects it.
func main() {
	var c char = '\101'
	_ = c
}
