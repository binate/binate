package "main"

// lex.escape.unsupported: the eight-digit \U Unicode escape is NOT supported
// and is rejected as an unknown escape sequence.
func main() {
	var s @[]readonly char = "\U00000041"
	_ = s
}
