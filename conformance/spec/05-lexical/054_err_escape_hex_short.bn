package "main"

// lex.escape.unsupported: a \x escape requires exactly two hex digits; \x4 has
// only one and is rejected (the lower boundary of the two-digit \xHH rule).
func main() {
	var c char = '\x4'
	_ = c
}
