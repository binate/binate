package "main"

// lex.literal.char.one (Constraint): an empty character literal '' denotes zero
// bytes and is ill-formed, so it is rejected at compile time ("character literal
// must be one byte"; §5.10 note, §5.14).
func main() {
	var c char = ''
	_ = c
}
