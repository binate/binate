package "main"
import "pkg/builtins/testing"

// lex.ident.form: identifiers are case-sensitive; `aA` and `Aa` are distinct names.
func main() {
    var aA int = 4
    var Aa int = 5
    if aA == 4 && Aa == 5 && aA != Aa { testing.Println("ok") } else { testing.Println("FAIL") }
}
