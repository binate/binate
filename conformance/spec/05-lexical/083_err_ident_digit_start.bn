package "main"

// lex.ident.form: an identifier must BEGIN with a letter or underscore, never a
// digit; `9x` is lexed as the INT literal `9` (then `x`), so it is rejected here.
func main() {
    var 9x int = 5
    _ = 9x
}
