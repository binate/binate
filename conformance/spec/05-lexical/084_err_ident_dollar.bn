package "main"

// lex.ident.form: `$` is not an identifier byte (ASCII letters/digits/`_` only);
// after `g` it is an ILLEGAL token, so the declaration is rejected.
var g$ int = 3

func main() {
    _ = g
}
