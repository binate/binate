package "main"

// lex.keywords.reserved: `func` is a reserved keyword and cannot be used as an
// ordinary identifier; using it where an identifier is required is rejected.
func main() {
    var func int = 5
    _ = func
}
