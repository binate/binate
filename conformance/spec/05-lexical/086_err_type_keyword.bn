package "main"

// lex.keywords.reserved: `type` is a reserved keyword and cannot be used as an
// ordinary identifier; using it where an identifier is required is rejected.
func main() {
    var type int = 5
    _ = type
}
