package "main"

// lex.keywords.reserved: `readonly` is a reserved keyword (the type modifier,
// distinct from `const`) and cannot be used as an ordinary identifier.
func main() {
    var readonly int = 5
    _ = readonly
}
