package "main"

// lex.keywords.reserved: `in` is a reserved keyword and cannot be used as an
// ordinary identifier; using it where an identifier is required is rejected.
func main() {
    var in int = 5
    _ = in
}
