package "main"

// lex.keywords.reserved: `struct` is a reserved keyword and cannot be used as an
// ordinary identifier; using it where an identifier is required is rejected.
func main() {
    var struct int = 5
    _ = struct
}
