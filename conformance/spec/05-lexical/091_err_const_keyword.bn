package "main"

// lex.keywords.reserved: `const` is a reserved keyword (the compile-time constant
// declaration, distinct from `readonly`) and cannot be used as an ordinary identifier.
func main() {
    var const int = 5
    _ = const
}
