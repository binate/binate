package "main"

// lex.builtins.set: `make` is a reserved builtin-operation keyword and cannot
// name an ordinary variable; using it as an identifier is rejected.
func main() {
    var make int = 5
    _ = make
}
