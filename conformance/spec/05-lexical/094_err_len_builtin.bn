package "main"

// lex.builtins.set: `len` is a reserved builtin-operation keyword and cannot
// name an ordinary variable; using it as an identifier is rejected.
func main() {
    var len int = 5
    _ = len
}
