package "main"

// lex.builtins.set: `sizeof` is a reserved builtin-operation keyword (takes a
// type argument) and cannot name an ordinary variable; rejected as an identifier.
func main() {
    var sizeof int = 5
    _ = sizeof
}
