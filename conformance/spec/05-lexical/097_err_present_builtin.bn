package "main"

// lex.builtins.set: `present` is a reserved builtin-operation keyword and cannot
// name an ordinary variable; using it as an identifier is rejected.
func main() {
    var present int = 5
    _ = present
}
