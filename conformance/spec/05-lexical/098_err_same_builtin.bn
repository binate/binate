package "main"

// lex.builtins.set: `same` is a reserved builtin-operation keyword and cannot
// name an ordinary variable; using it as an identifier is rejected.
func main() {
    var same int = 5
    _ = same
}
