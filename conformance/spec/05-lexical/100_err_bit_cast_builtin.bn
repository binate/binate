package "main"

// lex.builtins.set: `bit_cast` is a reserved builtin-operation keyword and cannot
// name an ordinary variable; using it as an identifier is rejected.
func main() {
    var bit_cast int = 5
    _ = bit_cast
}
