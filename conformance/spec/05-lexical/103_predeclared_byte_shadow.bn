package "main"

// lex.predeclared.are-idents: `byte` is predeclared, not a keyword; it may be
// shadowed by a user variable.
func main() {
    var byte int = 8
    if byte == 8 { println("ok") } else { println("FAIL") }
}
