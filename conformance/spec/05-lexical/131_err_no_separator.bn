package "main"

// lex.whitespace: without intervening whitespace, "var" and "x" merge into the
// single identifier "varx"; "varx int = 5" is then ill-formed (no var keyword).
func main() {
	varx int = 5
}
