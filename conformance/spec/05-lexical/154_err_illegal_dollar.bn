package "main"

// lex.error.illegal: '$' cannot begin any token, so the lexer emits an ILLEGAL
// token; here it appears where an expression is required, and the parser cannot
// start an expression from it -- the program is ill-formed.
func main() {
	var x int = $
	_ = x
}
