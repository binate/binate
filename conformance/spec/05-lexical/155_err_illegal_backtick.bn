package "main"

// lex.error.illegal: the backtick '`' cannot begin any token (Binate has no
// raw-string literals); it is an illegal character. As a statement-position
// token it cannot start an expression, so the program is ill-formed.
func main() {
	`
}
