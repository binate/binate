package "main"

// const.untyped: a CHARACTER literal is TYPED char (== uint8), not untyped, so
// it does NOT coerce to another integer type without a cast. var x int32 = 'A'
// is rejected; cast(int32, 'A') is the neighbor that compiles.
func main() {
	var x int32 = 'A'
	_ = x
}
