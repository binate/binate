package "main"

// const.int.range (Constraint): a literal at or above 2^64 is outside the union
// range [-2^63, 2^64-1] and is rejected at compile time. 0x10000000000000000 ==
// 2^64 is one past the largest valid literal.
func main() {
	var x uint64 = 0x10000000000000000
	_ = x
}
