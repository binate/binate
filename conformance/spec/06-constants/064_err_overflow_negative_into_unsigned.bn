package "main"

// const.overflow (§6.7): -1 is below the minimum of every unsigned type
// (whose range starts at 0), so assigning it to a 64-bit unsigned target is
// a compile-time error. The in-range neighbor 0 (or the max) compiles.

func main() {
	var x uint64 = -1
	_ = x
}
