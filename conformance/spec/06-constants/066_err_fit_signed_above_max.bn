package "main"

// const.expr.fit (§6.4): a signed n-bit target requires the value to lie in
// [-2^(n-1), 2^(n-1)-1]. 128 is one past the signed-8-bit maximum (127), so
// it does not fit and is rejected. The boundary 127 compiles (see 061).

func main() {
	var x int8 = 128
	_ = x
}
