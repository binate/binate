package "main"

// const.expr.fit (§6.4): a signed n-bit target requires the value to lie in
// [-2^(n-1), 2^(n-1)-1]. -129 is one below the signed-8-bit minimum (-128),
// so it does not fit and is rejected. The boundary -128 compiles (see 061).

func main() {
	var x int8 = -129
	_ = x
}
