package "main"

// const.float.no-int-mix (§6.5): an untyped integer constant is NOT
// implicitly converted to a floating-point type. Assigning the int constant
// 5 to a float64 destination is a type error; an explicit cast is required.

func main() {
	var f float64 = 5
	_ = f
}
