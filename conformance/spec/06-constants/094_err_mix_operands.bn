package "main"

// const.float.no-int-mix (§6.5): mixing a floating-point operand (1.5) with
// an integer operand (2) in the same expression, without an explicit cast,
// is a type error -- the int is not implicitly widened to float.

func main() {
	var f float64 = 1.5 + 2
	_ = f
}
