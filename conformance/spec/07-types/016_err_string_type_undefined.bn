package "main"

// type.scalar.universe (Constraint): there is NO `string` type in Binate
// (text is [N]readonly char / @[]readonly char). Using `string` as a type
// must be REJECTED -- `string` is an undefined identifier, not a
// predeclared scalar.

func main() {
	var s string = nil
	_ = s
}
