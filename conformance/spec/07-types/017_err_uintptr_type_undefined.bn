package "main"

// type.scalar.universe (Constraint): there is NO `uintptr` type -- `uint`
// serves that role since pointer size equals word size on every target.
// Using `uintptr` as a type must be REJECTED as an undefined identifier.

func main() {
	var p uintptr = 0
	_ = p
}
