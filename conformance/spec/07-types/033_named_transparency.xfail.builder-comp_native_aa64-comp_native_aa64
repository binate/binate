a distinct named type over a managed-slice (`type Buf @[]int`) miscompiles on the native aarch64 backend: index/len/slice/assignment of the named managed-slice produce wrong output or crash (works on LLVM, VM, gen1, gen2, and the 32-bit ARM native backend). claude-todo: native-aa64-named-managed-slice
