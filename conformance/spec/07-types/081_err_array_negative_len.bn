package "main"
import "pkg/builtins/testing"

// type.array.form (constraint): a NEGATIVE array length is rejected -- it has
// no valid layout. The length is a const so the checker can see it.

const N int = -2

func main() {
	var a [N]int
	testing.Println(len(a))
}
