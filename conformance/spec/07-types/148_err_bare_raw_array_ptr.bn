package "main"

// type.ptr.array-parens (negative): the bare `*[N]T` form is a syntax
// error -- `*[` is raw-slice sugar (`*[]T`), so a pointer to an array must
// be parenthesized `*([N]T)`. The parser rejects the bare form.

func main() {
	var p *[3]int
	_ = p
}
