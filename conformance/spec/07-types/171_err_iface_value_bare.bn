package "main"

// type.iface.value-spelling (constraint): a bare interface name is NOT a
// usable value type. `var g Greeter` is an error directing the user to
// the pointer-shaped forms `*Greeter` / `@Greeter`.

interface Greeter {
	greet()
}

func main() {
	var g Greeter
	_ = g
}
