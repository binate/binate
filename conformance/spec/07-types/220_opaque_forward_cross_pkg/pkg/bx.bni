package "pkg/bx"

// Box is FORWARD-declared (bare identifier, no `=`, no `struct`, no underlying
// type) — the syntax for an opaque export.  Callers see Box as a named type
// with no visible layout; the full body is in pkg/bx/box.bn.
type Box

func New(v int) @Box
func Get(b @Box) int
