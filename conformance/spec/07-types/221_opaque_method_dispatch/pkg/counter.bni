package "pkg/counter"

// Counter is forward-declared (opaque export).  A .bni may declare methods on
// a forward-declared type with a pointer or managed-handle receiver; here a
// @Counter receiver.  Callers dispatch through these methods.
type Counter

func New(base int) @Counter
func (c @Counter) Value() int
