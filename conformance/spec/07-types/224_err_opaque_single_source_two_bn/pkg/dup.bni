package "pkg/dup"

// Pair is exported opaquely (forward-declared); its full body must appear in
// exactly one .bn — but it is duplicated across a.bn and b.bn below.
type Pair

func New(a int, b int) @Pair
func Sum(p @Pair) int
