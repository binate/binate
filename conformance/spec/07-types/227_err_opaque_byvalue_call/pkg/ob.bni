package "pkg/ob"

// Box is opaque to importers.  MakeVal exports it BY VALUE -- an incoherent
// signature for an importer (who cannot hold Box by value), but the provider
// can write it because Box is transparent within its own package.
type Box

func MakeVal() Box
