package "main"
import "pkg/builtins/testing"

// conv.no-implicit-numeric: no implicit conversion between distinct scalar types
// (here signed -> unsigned); a cast is required.
func main() {
	var x int = 5
	var u uint = x
	testing.Println(cast(int, u))
}
