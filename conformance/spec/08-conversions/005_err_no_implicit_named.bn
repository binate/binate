package "main"

// conv.no-implicit-named: a named scalar type never converts implicitly to its
// underlying — Celsius -> float64 requires a cast.
type Celsius float64

func main() {
	var c Celsius = 1.0
	var f float64 = c
	println(cast(int, f))
}
