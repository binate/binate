package "main"
import "pkg/builtins/testing"

// conv.readonly: dropping element-level readonly is NOT implicit — an implicit
// *readonly int -> *int is rejected (use a cast; see 002).
func main() {
	var x int = 5
	var rp *readonly int = &x
	var p *int = rp
	testing.Println(*p)
}
