package "main"

// conv.no-implicit-numeric: there is no implicit conversion between integer and
// floating-point types — int -> float64 requires a cast.
func main() {
	var x int = 5
	var f float64 = x
	println(cast(int, f))
}
