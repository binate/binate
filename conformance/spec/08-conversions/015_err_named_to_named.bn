package "main"

// conv.no-implicit-named: two named types never inter-convert implicitly, even
// with identical underlying types (Celsius -> Fahrenheit requires a cast).
type Celsius float64
type Fahrenheit float64

func main() {
	var c Celsius = 1.0
	var x Fahrenheit = c
	println(cast(int, cast(float64, x)))
}
