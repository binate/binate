package "main"

// decl.const: a constant has no storage and no address, so `&C` is rejected.
// The rule-satisfying neighbor (taking the address of a `var`) compiles; see
// 001_const_basics for the positive use of a const as a value.
const C int = 7

func main() {
	var p *int = &C
	_ = p
}
