package "main"

// decl.var.redeclare (Constraint, negative): at package scope, two top-level
// declarations with the same name are an error. The rule-satisfying neighbor
// (the two vars given distinct names) compiles; the duplicate `g` here does
// not.

var g int = 1
var g int = 2

func main() {
	_ = g
}
