package "main"

// decl.type.package-only (Constraint): `type` declarations are package-level
// only. A function-local `type` declaration is rejected. The rule-satisfying
// neighbor moves the same declaration to package scope (see 130/151) and
// compiles.
func main() {
	type X int
	var v X = 3
	_ = v
}
