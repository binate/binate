package "pkg/lib"

// The .bn defines calc with TWO parameters -- a parameter-count
// mismatch with lib.bni (which declares one).  The checker must reject
// this divergence.
func calc(a int, b int) int {
	return a + b
}
