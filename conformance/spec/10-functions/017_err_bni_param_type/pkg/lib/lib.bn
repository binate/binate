package "pkg/lib"

// The .bn defines calc's first parameter as bool -- a parameter-type
// mismatch with lib.bni (which declares int).  The checker must reject
// this divergence.
func calc(a bool) int {
	if a {
		return 1
	}
	return 0
}
