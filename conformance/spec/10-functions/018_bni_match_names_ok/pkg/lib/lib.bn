package "pkg/lib"

// The .bn names the same parameters x, y -- the names differ from the
// .bni but the signature TYPES (int, int) int match, so the definition
// is consistent with the declaration.
func calc(x int, y int) int {
	return x + y
}
