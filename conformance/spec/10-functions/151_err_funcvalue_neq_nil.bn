package "main"

// func.value.equality: != against nil is ALSO rejected for function values
// (the != form, mirroring the == form in 150). present(fv) is the idiom.

func add(a int, b int) int { return a + b }

func main() {
	var f *func(int, int) int = add
	if f != nil {
		println("bad")
	}
}
