package "main"
import "pkg/builtins/testing"

// iface.extend (constraint): the ancestor graph must be acyclic.
// Direct self-extension (`interface A : A`) is rejected explicitly --
// the interface symbol is in scope during parent resolution, so this
// is caught as `cannot extend itself` rather than `undefined`.

interface A : A {
	foo()
}

func main() {
	testing.Println("ignored")
}
