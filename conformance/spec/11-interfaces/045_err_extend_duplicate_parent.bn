package "main"
import "pkg/builtins/testing"

// iface.extend (constraint): listing the same parent twice in one `:`
// clause is rejected -- the ancestor graph must be well-formed.

interface P {
	foo()
}

interface A : P, P {
}

func main() {
	testing.Println("ignored")
}
