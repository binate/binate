package "pkg/glue"

import "pkg/shape"
import "pkg/widget"

// The (Widget, Talker) impl lives HERE, in a third package that is
// neither Widget's nor Talker's home (iface.crosspkg.no-orphan permits
// this). A construction site only sees it if it imports pkg/glue.
impl *widget.Widget : shape.Talker

func Touch() { }
