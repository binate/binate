package "pkg/widget"

// Widget declares the method `speak` (in its own package, per the
// method-package rule) but NOT the impl -- the `impl *Widget :
// shape.Talker` lives in the third package pkg/glue.
func (w *Widget) speak() int { return w.v }
