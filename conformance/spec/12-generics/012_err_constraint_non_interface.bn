package "main"

// gen.constraint (Constraint): a type-parameter constraint must be a
// single named interface or the bare `any`. A non-interface type
// (`int`) in constraint position is rejected by the type-checker.

func bad[T int](x T) T {
	return x
}

func main() {
	println("unreached")
}
