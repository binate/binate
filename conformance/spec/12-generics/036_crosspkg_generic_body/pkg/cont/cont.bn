package "pkg/cont"

// The generic Cell / Make / Get are defined entirely in cont.bni (their bodies
// travel in the header).  This .bn carries a non-generic sentinel so the
// package has an implementation surface alongside its interface.
func Sentinel() int { return 1 }
