package "main"

// expr.associativity: binary operators are left-associative, so
// `a - b - c` is `(a - b) - c` (= 5 here, not the right-assoc 9).
func main() {
	if 10 - 3 - 2 == 5 { println("ok") } else { println("FAIL") }
}
