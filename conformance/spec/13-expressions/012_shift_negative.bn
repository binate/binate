package "main"

// expr.shift.negative: a negative runtime shift count is a defined
// non-recoverable panic (a constant negative count is instead a compile error).
func negcount() int { return -1 }

func main() {
	var n int = negcount()
	println(1 << n)
}
