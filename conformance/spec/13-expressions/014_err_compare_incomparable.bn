package "main"

// expr.compare.incomparable: slices are never comparable with == / != (not even
// to nil). Presence is tested with len, not equality.
func main() {
	var a @[]int = @[]int{1, 2}
	var b @[]int = @[]int{1, 2}
	if a == b {
		println("bad")
	}
}
