package "main"
import "pkg/builtins/testing"

// expr.unary.addr: the operand of & must be addressable — a bare literal has no
// storage, so &5 is a compile error.
func main() {
	var p *int = &5
	testing.Println(*p)
}
