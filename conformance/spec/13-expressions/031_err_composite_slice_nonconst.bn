package "main"

// expr.composite.slice: a non-const raw-slice literal *[]T{...} is rejected —
// the writable raw form must be *[]readonly T{...} (or use @[]T{...}).
func main() {
	var r *[]int = *[]int{1, 2, 3}
	println(r[0])
}
