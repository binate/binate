package "main"
import "pkg/builtins/testing"

// expr.shift.negative: a CONSTANT negative shift count is a compile-time error
// (the runtime-count form is the panic in 012).
func main() {
	var x int = 1 << -1
	testing.Println(x)
}
