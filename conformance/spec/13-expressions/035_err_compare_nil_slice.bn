package "main"
import "pkg/builtins/testing"

// expr.compare.incomparable: a slice is not comparable even to nil (slices are
// nil-ASSIGNABLE but not nil-COMPARABLE — the asymmetry with pointers).
func main() {
	var s @[]int = @[]int{1, 2}
	if s == nil {
		testing.Println("bad")
	}
}
