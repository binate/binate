package "main"

// expr.composite.array.inferred-len: [...]T{...} infers the length from the
// element count. Not yet implemented (rejected at parse), so xfail.
func main() {
	var a [...]int = [...]int{10, 20, 30}
	if len(a) == 3 && a[2] == 30 { println("ok") } else { println("FAIL") }
}
