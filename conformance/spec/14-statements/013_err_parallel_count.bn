package "main"

// stmt.assign.parallel: the two expression lists must have matching arity. Two
// targets against a single RHS expression is rejected ("assignment count
// mismatch"). The neighbor `a, b = 1, 2` compiles.
func main() {
	var a int
	var b int
	a, b = 1
	println("unreachable")
}
