package "main"

// stmt.incdec (§14.5 Constraint): a `const` operand is rejected -- a constant has
// no storage to mutate. `C++` on a local const must be a compile error. The
// rule-satisfying neighbor (`x++` on a `var`) compiles.

func main() {
	const C int = 5
	C++
}
