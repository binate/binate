package "main"

// stmt.incdec (§14.5 Constraint): the operand must be of integer type. `b++` on a
// `bool` has no integer operand and must be REJECTED. The rule-satisfying
// neighbor (`x++` on an int) compiles.

func main() {
	var b bool = true
	b++
}
