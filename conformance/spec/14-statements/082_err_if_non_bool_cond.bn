package "main"
import "pkg/builtins/testing"

// stmt.if (stmt.if.cond): the condition must be of bool type -- there is no
// truthy/falsy coercion, so an int condition `if 3 { }` is REJECTED.
func main() {
	if 3 { testing.Println("x") }
}
