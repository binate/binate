package "main"
import "pkg/builtins/testing"

// stmt.if.no-init: Binate's `if` takes a BARE condition; there is no Go-style
// init clause, so `if x := f(); cond { }` is REJECTED at parse time.
func f() int { return 1 }
func main() {
	if x := f(); x > 0 { testing.Println("y") }
}
