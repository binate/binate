package "main"

// stmt.for.in: ranging over a non-iterable type (an int is neither slice,
// managed-slice, nor array) is REJECTED with "cannot range over non-iterable type".
func main() {
	var n int = 5
	for v in n {
		var _ int = v
	}
}
