package "main"
import "pkg/builtins/testing"

// stmt.return.arity: returning two values from a single-result function is
// rejected -- the returned-expression count must equal the result count.
func one() int {
	return 1, 2
}

func main() {
	testing.Println(one())
}
