package "main"

// stmt.return.arity: a bare `return` is legal only when the function has no
// results; here `one` declares an int result, so the bare return is rejected.
func one() int {
	if false {
		return 5
	}
	return
}

func main() {
	println(one())
}
