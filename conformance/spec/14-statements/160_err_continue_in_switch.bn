package "main"

// stmt.break: `continue` inside a switch that is not itself inside a loop is
// rejected -- continue requires an enclosing loop.
func main() {
	var n int = 1
	switch n {
	case 1:
		continue
	default:
	}
}
