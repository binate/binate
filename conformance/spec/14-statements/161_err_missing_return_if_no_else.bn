package "main"

// stmt.terminating: an `if` with no `else` never terminates, so a result-
// returning function whose only return is inside an if (no else) is rejected
// with "missing return".
func f(x int) int {
	if x > 0 {
		return x
	}
}

func main() {
	println(f(3))
}
