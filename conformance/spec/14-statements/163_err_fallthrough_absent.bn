package "main"

// stmt.absences: there is no `fallthrough` keyword; a switch case never falls
// through into the next, so `fallthrough` is undefined.
func main() {
	var n int = 1
	switch n {
	case 1:
		fallthrough
	case 2:
		println("two")
	default:
	}
}
