package "main"

// stmt.absences: there is no `recover` -- panic(...) is an unrecoverable
// abort and errors are values (Go-style multiple returns), so `recover` is
// undefined.
func main() {
	var x int = recover()
	println(x)
}
