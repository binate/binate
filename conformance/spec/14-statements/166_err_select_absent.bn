package "main"

// stmt.absences: there is no `select` statement (execution is single-threaded;
// no goroutines/channels/select). `select` is not a keyword -- it is an ordinary
// identifier that resolves to nothing, so `select { }` is rejected
// ("undefined: select"). The neighbor without `select` compiles.
func main() {
	select {
	}
}
