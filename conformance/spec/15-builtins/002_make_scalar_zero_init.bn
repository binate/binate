package "main"
import "pkg/builtins/testing"

// builtin.make: make(int) -> @int, the allocation zero-initialized; *p reads 0.
func main() {
	var p @int = make(int)
	var ok bool = true
	if *p != 0 { ok = false }
	if ok { testing.Println("ok") } else { testing.Println("FAIL") }
}
