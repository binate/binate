package "main"

// builtin.box: box(true) -> @bool (default type of the bool value); *b reads it.
func main() {
	var b @bool = box(true)
	var ok bool = true
	if *b != true { ok = false }
	if ok { println("ok") } else { println("FAIL") }
}
