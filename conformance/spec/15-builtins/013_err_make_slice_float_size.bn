package "main"

// builtin.make-slice: the size argument must be an integer value; a float size
// is rejected. (make_slice(int, 3) compiles.)
func main() {
	var s @[]int = make_slice(int, 3.5)
	_ = s
}
