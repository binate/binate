package "main"

// builtin.make: make(T) yields @T. make(Pt) is @Pt and is NOT assignable to a
// @int location. (make(Pt) -> @Pt compiles.)
type Pt struct {
	x int
}

func main() {
	var b @int = make(Pt)
	_ = b
}
