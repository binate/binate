package "main"

// builtin.box: box(v) yields @T for the default type of v. box(42) is @int and
// is NOT assignable to a @bool location. (box(42) -> @int compiles.)
func main() {
	var b @bool = box(42)
	_ = b
}
