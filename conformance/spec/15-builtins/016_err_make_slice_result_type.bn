package "main"

// builtin.make-slice: make_slice(T, n) yields @[]T. make_slice(int, n) is
// @[]int and is NOT assignable to a @[]bool location. (make_slice(int, n) ->
// @[]int compiles.)
func sz() int { return 3 }

func main() {
	var s @[]bool = make_slice(int, sz())
	_ = s
}
