bit_cast to a SUB-WORD type (uint32 on a 64-bit machine), used DIRECTLY (not stored into a typed local first), is not narrowed in the bytecode VM or the native aarch64 backend -- the high bits leak so bit_cast(uint32, int32 -1) reads as the full-width -1, not 4294967295. Correct on the LLVM backends and arm32; a stored bit_cast (var u uint32 = ...) narrows and is fine. claude-todo (bit_cast sub-word narrowing, line ~814).
