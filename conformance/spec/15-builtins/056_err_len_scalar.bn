package "main"
import "pkg/builtins/testing"

// builtin.len (Constraint): a scalar value type has no length -- len(x) on an
// int is rejected. The rule-satisfying neighbor len(array)/len(slice) compiles.

func main() {
	var x int = 5
	testing.Println(len(x))
}
