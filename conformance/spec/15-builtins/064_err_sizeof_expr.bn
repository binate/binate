package "main"

// builtin.sizeof (Constraint): sizeof takes a TYPE argument, not an expression
// (this is why it cannot be an ordinary function). Passing a value `x` is
// rejected with "x is not a type". The neighbor sizeof(int) compiles.

func main() {
	var x int = 5
	println(sizeof(x))
}
