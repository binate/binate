package "main"
import "pkg/builtins/testing"

// builtin.unsafe-index (Constraint): a scalar value type is not a collection --
// unsafe_index(x) on an int is rejected. The neighbor unsafe_index(array)
// compiles (see 070).

func main() {
	var x int = 5
	testing.Println(unsafe_index(x, 0))
}
