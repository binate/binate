package "main"

// builtin.present (Constraint): a STRUCT is a value type (never unset), so
// present of a struct value is REJECTED at compile time (§15.5).

type Pt struct { x int; y int }

func main() {
	var p Pt = Pt{1, 2}
	var r bool = present(p)   // struct value -> rejected
	_ = r
}
