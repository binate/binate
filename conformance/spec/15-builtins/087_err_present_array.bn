package "main"

// builtin.present (Constraint): an ARRAY [N]T is a value type (never unset),
// so present of an array value is REJECTED at compile time (§15.5).

func main() {
	var a [3]int
	var r bool = present(a)   // array value -> rejected
	_ = r
}
