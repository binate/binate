package "main"

// builtin.reserved (§15.1): each keyword-builtin always lexes as the built-in,
// so a program cannot declare a variable with that name -- `var make int` is a
// parse error "expected IDENT, got make". The neighbor `var notmake int`
// compiles.
func main() {
	var make int = 3
	_ = make
}
