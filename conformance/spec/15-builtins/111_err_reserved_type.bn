package "main"

// builtin.reserved (§15.1): a type declaration cannot use a keyword-builtin name
// either; `type box int` fails to parse ("expected IDENT, got box"). Neighbor:
// `type mybox int` compiles.
type box int

func main() {
}
