package "main"

// builtin.reserved (§15.1): a function declaration cannot use a keyword-builtin
// name; `func len()` fails to parse ("expected IDENT, got len"). Neighbor:
// `func mylen()` compiles.
func len() {
}

func main() {
}
