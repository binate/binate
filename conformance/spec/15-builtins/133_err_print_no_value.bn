package "main"

// builtin.print (§15.7): print/println yield NO value -- their result cannot be
// used in an expression. Assigning println(...) to a variable is rejected
// ("cannot assign void to int"). Neighbor: calling println(...) as a statement
// compiles.
func main() {
	var x int = println("hi")
	_ = x
}
