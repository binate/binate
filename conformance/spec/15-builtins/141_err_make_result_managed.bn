package "main"

// builtin.result-types (§15.1): make(T) yields @T (a managed pointer), not a
// bare T -- so it cannot be assigned to an int. Neighbor: `var x @int =
// make(int)` compiles.
func main() {
	var x int = make(int)
	_ = x
}
