package "main"

// builtin.result-types (§15.1): present(x) yields bool, not int -- assigning it
// to an int is rejected ("cannot assign bool to int"). Neighbor: `var b bool =
// present(p)` compiles.
func main() {
	var p @int = make(int)
	var b int = present(p)
	_ = b
}
