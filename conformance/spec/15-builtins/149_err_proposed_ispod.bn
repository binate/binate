package "main"

// builtin.proposed (§15.8): ispod(T) (a compile-time POD predicate) is PROPOSED,
// not implemented -- the identifier is undefined, so `ispod(int)` is "undefined:
// ispod". Neighbor: a real bool predicate like present(p) compiles.
func main() {
	var b bool = ispod(int)
	_ = b
}
