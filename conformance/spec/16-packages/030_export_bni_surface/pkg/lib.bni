package "pkg/lib"

// The .bni IS the exported surface.  `answer` (lowercase) and `Visible`
// appear here, so they are exported.  `Secret` is deliberately absent — it
// is package-private even though it is Capitalized, because the compiler
// determines visibility solely from .bni membership, not capitalization.
func answer() int
func Visible() int
