package "pkg/lib"

// Secret is declared ONLY here (no .bni entry) — package-private despite its
// capitalization.  It is freely usable within pkg/lib (answer calls it), but
// a cross-package reference to it is rejected (see 031).
func Secret() int { return 42 }

func answer() int { return Secret() }

func Visible() int { return 7 }
