package "pkg/lib"

// Point is authoritative in lib.bni (transparent) — NOT redefined here.
// Scale is the .bni's const — NOT redeclared here (no extern const).  Only
// Make's body lives in the .bn.
func Make(x int, y int) @Point {
	var p @Point = make(Point)
	p.X = x
	p.Y = y
	return p
}
