package "pkg/util"

// a.bn: defines the package-private base() used by b.bn's Plus(), and
// Twice() which calls helper() defined in b.bn — cross-file package
// visibility (the two files merge into one package).
func base() int { return 100 }

func Twice(n int) int { return 2 * helper(n) }
