package "pkg/util"

// b.bn: defines the package-private helper() used by a.bn's Twice(), and
// Plus() which calls base() defined in a.bn.  helper() is the identity so
// Twice(10) == 20; Plus(n) == base()+n.
func helper(n int) int { return n }

func Plus(n int) int { return base() + n }
