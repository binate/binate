package "main"

// prog.main.package / prog.main.signature: program execution begins in the main
// package, which requires a function `main` (and needs no .bni); the entry
// function is `func main()` -- no parameters and no results.
func main() {
	println("ok")
}
