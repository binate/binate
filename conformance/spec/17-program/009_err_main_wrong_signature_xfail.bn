package "main"

// prog.main.signature: the entry function is `func main()` with NO parameters
// and no results. A wrong-shaped main SHOULD be rejected (a link-time failure
// per §17.3.1). XFAIL: the entry synthesis does not enforce the signature --
// `func main(x int)` compiles, links, AND runs (the extra parameter is ignored),
// so this is silently accepted instead of rejected (claude-todo: main-signature
// not enforced).
func main(x int) {
	println("ran-with-param")
}
