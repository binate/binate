the entry-point main signature is NOT enforced: func main(x int) (and func main() int) compiles, links, and RUNS -- the extra parameter is ignored -- instead of being rejected as the wrong entry shape (§17.3.1 says a wrong-shaped main is a link-time failure). prog.main.signature; claude-todo (main-signature not enforced).
