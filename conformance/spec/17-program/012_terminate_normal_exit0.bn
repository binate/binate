package "main"

// prog.terminate: a program terminates normally when main returns. In the
// current implementation normal termination yields exit code 0 (the harness
// checks a clean exit and the expected stdout).
func main() {
	println("done")
}
