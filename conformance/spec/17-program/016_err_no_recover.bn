package "main"

// prog.panic.no-recover / prog.panic: there is no `recover` -- a panic is the
// program's last action and cannot be caught. `recover` is not a defined name,
// so calling it is rejected ("undefined: recover").
func main() {
	var x int = recover()
	_ = x
}
