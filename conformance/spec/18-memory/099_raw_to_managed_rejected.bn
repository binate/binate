package "main"

type T struct {
	V int
}

func main() {
	// mem.managed-vs-raw: a raw value is a borrow that holds NO count, so it
	// cannot create a new managed owner. Assigning a raw `*T` to a managed `@T`
	// must be rejected.
	var a @T = make(T)
	var r *T = a
	var owner @T = r
	owner.V = 1
}
