package "main"
import "pkg/builtins/testing"

// Comparable.Compare(other) returns 0 IFF equal, nonzero otherwise; at this
// level the sign/magnitude of a nonzero result carry NO meaning. This test
// checks the equality predicate only (not sign), across several primitives.
func eq(c int) int {
	if c == 0 { return 1 }
	return 0
}

func main() {
	var a int = 3
	var b int = 5
	testing.Println(eq(a.Compare(a)))   // 1: equal
	testing.Println(eq(a.Compare(b)))   // 0: unequal
	testing.Println(eq(b.Compare(a)))   // 0: unequal (order-independent equality)

	var t bool = true
	var f bool = false
	testing.Println(eq(t.Compare(t)))   // 1
	testing.Println(eq(t.Compare(f)))   // 0

	var u uint64 = 100
	var v uint64 = 100
	var w uint64 = 200
	testing.Println(eq(u.Compare(v)))   // 1: equal values in distinct vars
	testing.Println(eq(u.Compare(w)))   // 0
}
