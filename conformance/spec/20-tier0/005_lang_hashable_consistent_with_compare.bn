package "main"
import "pkg/builtins/testing"

// Hashable.Hash() is consistent with Compare's equality: a.Compare(b)==0
// implies a.Hash()==b.Hash(). Hash is also stable across calls.
func yn(b bool) int {
	if b { return 1 }
	return 0
}

func main() {
	var a int = 42
	var b int = 42
	var c int = 99
	testing.Println(yn(a.Compare(b) == 0))      // 1: equal
	testing.Println(yn(a.Hash() == b.Hash()))   // 1: consistent
	testing.Println(yn(a.Hash() == a.Hash()))   // 1: stable
	testing.Println(yn(a.Compare(c) == 0))      // 0: unequal
	testing.Println(yn(a.Hash() == c.Hash()))   // 0: distinct under the bit-pattern impl (primitives), not a Hashable guarantee

	var t1 bool = true
	var t2 bool = true
	testing.Println(yn(t1.Compare(t2) == 0))    // 1
	testing.Println(yn(t1.Hash() == t2.Hash())) // 1
}
