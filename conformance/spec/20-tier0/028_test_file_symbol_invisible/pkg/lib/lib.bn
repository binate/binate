package "pkg/lib"

// Greeting() calls TestHelper(), which is defined ONLY in lib_test.bn.  In a
// normal build lib_test.bn is excluded, so TestHelper is undefined here.
func Greeting() @[]char { return TestHelper() }
