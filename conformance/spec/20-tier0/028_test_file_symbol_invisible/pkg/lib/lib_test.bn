package "pkg/lib"

import "pkg/builtins/testing"

// TestHelper() and the test function TestGreeting() live only in this _test.bn
// file.  Excluded from the normal build, so lib.bn's reference to TestHelper is
// "undefined" in a normal build.
func TestHelper() @[]char { return "hi" }

func TestGreeting() testing.TestResult {
	return make_slice(char, 0)
}
