VM cross-mode float64 value marshaling through the injected pkg/std/math boundary (Float64bits/Abs/Signbit/Copysign/IsNaN/IsInf/Floor/Ceil/Trunc/Round) misclassifies on an x86-64 host VM, so a classify check reads 0. Passes on an aarch64 host VM and on all compiled backends — x64-host-specific. Distinct from the time.Point int64 bug; see claude-todo.md (VM float64 marshaling, x64-host).
