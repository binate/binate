os.Seek is LOWERED to bytecode under int (its __c_call is indirect via the unexported cLseek wrapper, so funcHasCCall misses it), shadowing the injected native Seek; the lowered bytecode then cannot reach cLseek ("vm: extern not found: pkg/std/os.cLseek"). Unlike 092_ccall_basic (raw __c_call in user code, a permanent VM-FFI non-goal), os supplies injection, so Seek should be served by it. Fix: transitive lowering-skip (or inline the off_t branch). Tracked in claude-todo.md.
