chaining a method on the by-value struct result of a cross-package method (fi.ModTime().ToUnix()) emits invalid IR (extractvalue on a scalar); compile error in every mode. See claude-todo. Workaround: bind to a var first.
