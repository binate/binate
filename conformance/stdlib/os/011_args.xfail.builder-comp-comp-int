Interpreted mode: pkg/std/os is injected as host-native code, so os.Args() runs natively and returns the host interpreter (bni)'s own argv (its -I/-L/program.bn tokens) instead of the program's — it reaches bni's native bootstrap.Args() and never sees the VM arg path. Correct in compiled mode; cross-mode plumbing fix pending (claude-todo.md).
