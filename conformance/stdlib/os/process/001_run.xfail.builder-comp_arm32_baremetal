os/process needs a process model (fork/exec/wait); arm32 bare-metal has none, so Run returns errors.Unsupported and running /usr/bin/true can never succeed on a freestanding target.  The native arm32 baremetal mode inherits this via OVERRIDE_MODE.
