arm32-baremetal probe under qemu-system-arm.  Targeted to the linux-x64 e2e
runner: the arm32 emulation is run on the cheap/plentiful Linux lane rather than
the ~10x-cost macOS lane (qemu-system-arm could run on macOS, but we deliberately
don't schedule arm32 work there).
