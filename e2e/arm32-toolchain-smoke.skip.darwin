arm32-LINUX tools run under qemu-user, which emulates the Linux syscall ABI and
therefore cannot run on macOS at all; this smoke targets the linux-x64 e2e runner.
See explorations/plan-arm32-toolchain-smoke.md.
