package "main"

import "pkg/stdx/fmt"

// selftest.bn — A simple program to verify the self-hosted interpreter works.
// Run via: binate main.bn -- selftest.bn

func main() {
	fmt.Println("=== Binate Self-Test ===")

	// 1. Integer arithmetic
	var x int = 2 + 3
	if x == 5 {
		fmt.Println("PASS: arithmetic")
	} else {
		fmt.Println("FAIL: arithmetic")
	}

	// 2. String
	var s *[]readonly char = "hello"
	fmt.Println(s)

	// 3. Boolean
	var b bool = true
	if b {
		fmt.Println("PASS: bool true")
	} else {
		fmt.Println("FAIL: bool true")
	}

	// 4. For loop
	var sum int = 0
	for i := 0; i < 5; i++ {
		sum = sum + i
	}
	if sum == 10 {
		fmt.Println("PASS: for loop")
	} else {
		fmt.Println("FAIL: for loop")
	}

	// 5. Function call
	var r int = double(7)
	if r == 14 {
		fmt.Println("PASS: func call")
	} else {
		fmt.Println("FAIL: func call")
	}

	// 6. Recursion
	var f int = fib(10)
	if f == 55 {
		fmt.Println("PASS: recursion (fib)")
	} else {
		fmt.Println("FAIL: recursion (fib)")
	}

	// 7. Managed slice + make_slice
	var sl @[]int = make_slice(int, 3)
	sl[0] = 10
	sl[1] = 20
	sl[2] = 30
	if len(sl) == 3 {
		fmt.Println("PASS: make_slice + len")
	} else {
		fmt.Println("FAIL: make_slice + len")
	}
	if sl[1] == 20 {
		fmt.Println("PASS: slice index")
	} else {
		fmt.Println("FAIL: slice index")
	}

	fmt.Println("=== Self-Test Complete ===")
}

func double(n int) int {
	return n * 2
}

func fib(n int) int {
	if n <= 1 {
		return n
	}
	return fib(n - 1) + fib(n - 2)
}
