// Package startup is the platform entry/startup glue.  It is the startup
// counterpart to `rt`'s runtime *services* (alloc/refcount/exit): `rt` is
// services, `startup` is startup.  It is force-included in every hosted link
// (via cmd/bnc's ensureRuntimeDepsLoaded) so the startup/entry glue can live
// here in Binate rather than in runtime/binate_runtime.c.
//
// Today it holds the command-line-argument seam — `Args` and `SetArgs`, which
// `pkg/std/os` re-exports (os.Args/os.SetArgs delegate here; os imports
// startup).  Argv belongs to startup because it is an entry concern: the
// eventual hosted `main` (design-ffi-export.md §3.3) will set `Args` directly
// from the real argv and retire `bootstrap.Args`.
//
// The remaining entry glue — the hosted `main` (capturing argc/argv), the
// baremetal `_start`, and a C-library `_init`, selected per target by `#[build]`
// — is still to come, and can only land once a BUILDER that force-includes
// `startup` is pinned (gen1 is `cmd/bnc` linked BY BUILDER; the entry `main` is
// the program root, force-included, not reachable via the import graph, so it
// needs BUILDER's force-load set to carry `startup`).  See
// plan-ffi-export-detailed.md Phase 6.  (The args seam above does NOT need that
// re-pin: it rides os's import of startup, which BUILDER already follows.)
package "pkg/builtins/startup"

// Args holds the program's command-line arguments, indexed like Go's os.Args:
// element 0 the program name, elements 1.. the arguments.  Fully readonly (the
// slice and its argument text), so it is safe to share the same slice with every
// reader; only a reassignment (SetArgs, or the future entry glue) swaps it.
// os.Args() returns this directly.
var Args @[]readonly @[]readonly char

// SetArgs replaces Args and returns the PREVIOUS value, so they can be saved and
// restored: `old := SetArgs(new); …; SetArgs(old)` (the returned value flows
// straight back in).  newArgs is the whole argv — element 0 the program name,
// 1.. the arguments — so the caller controls index 0.  It is the seam a bytecode
// interpreter (cmd/bni) uses to give an interpreted program its own argv (via
// os.SetArgs, since os is injected host-native) — see
// explorations/design-os-args-vm.md.
func SetArgs(newArgs @[]readonly @[]readonly char) @[]readonly @[]readonly char

// Env returns the process environment as "NAME=value" strings (POSIX environ
// order), a fully-readonly managed slice safe to share.  os.Env() delegates here.
// A stub today: empty by default and until a data source (a bootstrap primitive
// or the entry glue) populates it — only SetEnv changes it.  (The backing `env`
// global is unexported; read it through this accessor.)
func Env() @[]readonly @[]readonly char

// SetEnv replaces the environment Env() returns and returns the PREVIOUS value,
// so it can be saved and restored: `old := SetEnv(new); …; SetEnv(old)` (the
// returned value flows straight back in).  Each entry is a "NAME=value" string.
// The env counterpart of SetArgs — the seam a bytecode interpreter (cmd/bni)
// uses to give an interpreted program its own environment (via os.SetEnv, since
// os is injected host-native) — see explorations/design-os-args-vm.md.
func SetEnv(newEnv @[]readonly @[]readonly char) @[]readonly @[]readonly char
