# `impls/stdlib/` — tier 1 + 1x implementations

Holds `.bn` files for the bundled standards library (tier 1) and its
standards-track companion (tier 1x). Per-platform leaves match
`impls/core/`'s layout: `common/`, `libc/`, and (eventually)
`baremetal/`.

Currently empty — tier 1 / 1x packages don't exist yet.
