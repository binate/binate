package "pkg/stdx/slices"

// All of pkg/stdx/slices' surface is generic, and Binate monomorphizes
// generics at the call site — so the bodies live in pkg/stdx/slices.bni
// (body-included) where both in-package tests and cross-package
// consumers can specialize them.  This implementation file is
// intentionally empty beyond the package declaration.
