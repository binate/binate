package "pkg/std/iter"

// pkg/std/iter's surface is the generic iteration protocol declared
// body-included in pkg/std/iter.bni, which consumers monomorphize at
// their own call sites.  This implementation file is intentionally empty beyond the
// package declaration.
