package "pkg/stdx/containers/iter"

// pkg/stdx/containers/iter's surface is the generic iteration protocol declared
// body-included in pkg/stdx/containers/iter.bni, which consumers monomorphize at
// their own call sites.  This implementation file is intentionally empty beyond the
// package declaration.
