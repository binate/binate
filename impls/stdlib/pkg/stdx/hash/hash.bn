package "pkg/stdx/hash"

// All of pkg/stdx/hash's surface is generic, and Binate monomorphizes generics at
// the call site — so the bodies live in pkg/stdx/hash.bni (body-included) where both
// in-package tests and cross-package consumers can specialize them.  This
// implementation file is intentionally empty beyond the package declaration.
