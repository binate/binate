package "main"

// No-op. Measures startup overhead: in interpreter modes, the cost
// of loading/parsing/type-checking the program and warming up the
// interpreter; in compiled modes, the cost of invoking the binary.

func main() {}
