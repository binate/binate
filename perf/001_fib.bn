package "main"

import "pkg/stdx/fmt"

// Recursive Fibonacci. Measures function-call overhead and basic
// control-flow codegen. fib(30) = 832040.

func fib(n int) int {
	if n < 2 {
		return n
	}
	return fib(n - 1) + fib(n - 2)
}

func main() {
	fmt.Println(fib(30))
}
