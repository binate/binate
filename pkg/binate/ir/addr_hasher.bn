package "pkg/binate/ir"

// Hash is AddrHasher's method (see the declaration).
func (h AddrHasher) Hash(k int) uint {
	var x uint = cast(uint, k)
	x = (x ^ (x >> 16)) * cast(uint, 0x45d9f3b)
	x = (x ^ (x >> 16)) * cast(uint, 0x45d9f3b)
	return x ^ (x >> 16)
}
