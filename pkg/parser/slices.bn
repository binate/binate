package "pkg/parser"

import "pkg/ast"

// Per-type append helpers for the parser.
// These use O(n) copy-per-append, which is acceptable for parser output sizes.

// appendParseError appends a ParseError to a managed slice, returning a new slice.
func appendParseError(s @[]ParseError, v ParseError) @[]ParseError {
	var n int = len(s)
	var ns @[]ParseError = make_slice(ParseError, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendFieldDecl appends a FieldDecl to a managed slice, returning a new slice.
func appendFieldDecl(s @[]@ast.FieldDecl, v @ast.FieldDecl) @[]@ast.FieldDecl {
	var n int = len(s)
	var ns @[]@ast.FieldDecl = make_slice(@ast.FieldDecl, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendImportSpec appends an ImportSpec to a managed slice, returning a new slice.
func appendImportSpec(s @[]@ast.ImportSpec, v @ast.ImportSpec) @[]@ast.ImportSpec {
	var n int = len(s)
	var ns @[]@ast.ImportSpec = make_slice(@ast.ImportSpec, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendDecl appends a Decl to a managed slice, returning a new slice.
func appendDecl(s @[]@ast.Decl, v @ast.Decl) @[]@ast.Decl {
	var n int = len(s)
	var ns @[]@ast.Decl = make_slice(@ast.Decl, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendStmt appends a Stmt to a managed slice, returning a new slice.
func appendStmt(s @[]@ast.Stmt, v @ast.Stmt) @[]@ast.Stmt {
	var n int = len(s)
	var ns @[]@ast.Stmt = make_slice(@ast.Stmt, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendExpr appends an Expr to a managed slice, returning a new slice.
func appendExpr(s @[]@ast.Expr, v @ast.Expr) @[]@ast.Expr {
	var n int = len(s)
	var ns @[]@ast.Expr = make_slice(@ast.Expr, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendParamDecl appends a ParamDecl to a managed slice, returning a new slice.
func appendParamDecl(s @[]@ast.ParamDecl, v @ast.ParamDecl) @[]@ast.ParamDecl {
	var n int = len(s)
	var ns @[]@ast.ParamDecl = make_slice(@ast.ParamDecl, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendTypeExpr appends a TypeExpr to a managed slice, returning a new slice.
func appendTypeExpr(s @[]@ast.TypeExpr, v @ast.TypeExpr) @[]@ast.TypeExpr {
	var n int = len(s)
	var ns @[]@ast.TypeExpr = make_slice(@ast.TypeExpr, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendElement appends an Element to a managed slice, returning a new slice.
func appendElement(s @[]@ast.Element, v @ast.Element) @[]@ast.Element {
	var n int = len(s)
	var ns @[]@ast.Element = make_slice(@ast.Element, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}

// appendCaseClause appends a CaseClause to a managed slice, returning a new slice.
func appendCaseClause(s @[]@ast.CaseClause, v @ast.CaseClause) @[]@ast.CaseClause {
	var n int = len(s)
	var ns @[]@ast.CaseClause = make_slice(@ast.CaseClause, n + 1)
	for i := 0; i < n; i++ { ns[i] = s[i] }
	ns[n] = v
	return ns
}
