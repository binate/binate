# Per-import exemptions for conformance-imports.sh.
# One `<conformance-relative-path>:<import-path>` entry per line.
# Comments start with #.

# 240_err_import_nonexistent.bn deliberately imports a missing package
# to verify the loader's error path; the import never resolves.
240_err_import_nonexistent.bn:pkg/does_not_exist

# 411_pkg_builtins_lang_stringer.bn exercises the stdlib carve-out
# (plan-primitives-impl-interfaces.md): canonical Stringer impls
# for primitives live in pkg/builtins/lang and the test imports them.
411_pkg_builtins_lang_stringer.bn:pkg/builtins/lang

# 412_pkg_builtins_lang_compare.bn / 413_err_pkg_builtins_lang_iv_compare.bn
# exercise pkg/builtins/lang's Comparable / Orderable / Hashable
# (plan-primitives-impl-interfaces.md Slice 2b).
412_pkg_builtins_lang_compare.bn:pkg/builtins/lang
413_err_pkg_builtins_lang_iv_compare.bn:pkg/builtins/lang

# 414_pkg_builtins_lang_hash_iv.bn: iv dispatch to Hashable.Hash (no
# Self) — the positive complement to 413's Compare-rejection.
414_pkg_builtins_lang_hash_iv.bn:pkg/builtins/lang

# 415_pkg_builtins_lang_char_byte.bn: confirms char / byte (aliases of
# uint8) inherit pkg/builtins/lang's uint8 impls.
415_pkg_builtins_lang_char_byte.bn:pkg/builtins/lang

# 434_generic_constraint_method.bn: end-to-end constraint-method
# dispatch through a generic instantiation (plan-generics.md
# Slice 4b).  Uses pkg/builtins/lang's Comparable / Hashable so the
# satisfaction-through-inheritance path (Orderable extends
# Comparable, Hashable extends Comparable) covers the
# canonical use case.
434_generic_constraint_method.bn:pkg/builtins/lang

# 064_bootstrap_funcs.bn now also imports pkg/binate/buf since the
# Concat helper migrated out of pkg/bootstrap (binate
# replaces-bootstrap-Concat).  Itoa is still under pkg/bootstrap.
064_bootstrap_funcs.bn:pkg/binate/buf
