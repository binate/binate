native static-data managed-pointer addend bug: RegisterStandardExterns walks rt._Package().Functions and derefs a static @T header-pointer as payload (FunctionInfo.Name.ptr=0 -> SIGSEGV) because static-data @T relocs omit +headerSize. PRE-EXISTING; exposed once the weak-dtor coalescing fix let these packages link natively. See claude-todo CRITICAL (native static-data managed-pointer addend).
