imports pkg/std/os + pkg/std/os/sys (interp/externs.bn); the POSIX syscall wrappers are undefined on baremetal (no OS/syscalls), so the test binary cannot link under arm32-baremetal QEMU semihosting. The interp codegen is fine — it runs on the hosted modes. See claude-todo.md.
