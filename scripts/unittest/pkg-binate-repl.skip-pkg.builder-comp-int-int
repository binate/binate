repl builds on interp.New() (full standard-package-set injection per test), so under double-VM it has the same ~490s/test pathology as pkg/binate/interp and blows the 45min shard cap. Double-VM adds ~no coverage over -int for the REPL over the interpreter it drives. Runs fully under -int / -comp-int / -comp. (See pkg-binate-interp.skip-pkg.builder-comp-int-int.)
