links pkg/std/os/sys (POSIX syscall wrappers) for its I/O; those symbols are undefined on baremetal (no OS/syscalls), so the test binary cannot link under arm32-baremetal QEMU semihosting. The repl codegen is fine — it compiles and passes on the hosted modes (e.g. native_x64: 73 passed). See claude-todo.md.
