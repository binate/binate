compile-heavy under the VM (its tests each drive a full compile+run); split its tests across shards via bni --test --shard-index/--shard-count so it does not time out one shard — see claude-todo "int-int slow-package skips" -> test-level sharding
