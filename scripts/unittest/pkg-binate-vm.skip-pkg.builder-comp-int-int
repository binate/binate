vm's own test suite takes >24 min under double-VM (its tests, as bytecode, run by a bni that is itself bytecode under another bni), timing out the shard. The VM's logic is covered by single-VM modes (builder-comp-int / -comp-int); the double-VM lane's unique value is exercised by every OTHER package running under it. Re-add after optimizing vm's tests under double-VM (or per-test-skip its slowest) — see claude-todo "int-int slow-package skips"
