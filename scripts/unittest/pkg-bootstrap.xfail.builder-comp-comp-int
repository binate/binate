bootstrap is native-only in the VM (cmd/bni skips lowering it: its I/O is libc-backed via the C runtime, and its format helpers are resolved as registered externs); its bytecode unit tests do not apply in VM-leg modes. Covered by the compiled modes + conformance print/println exercising the native format helpers. See plan-bootstrap-ccall.md.
