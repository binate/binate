rt is native-only in the VM (its bodies call libc via __c_call, which the VM cannot interpret); its bytecode unit tests do not apply in VM-leg modes. Covered by the compiled modes + every other -int test exercising the native rt. See plan-rt-ccall-drop-libc.md.
