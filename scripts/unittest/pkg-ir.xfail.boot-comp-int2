boot-comp-int2: TestGenConstIota — moduleConsts[i].Name reads back empty for all but last entry after appendModuleConst-style copy-grow. genConstGroup correctly emits 0,1,2 but lookupConst sees Name="", "", "C". Suspect VM struct-copy via BC_MEMCPY doesn't preserve @[]char fields (refcount or alias issue).
