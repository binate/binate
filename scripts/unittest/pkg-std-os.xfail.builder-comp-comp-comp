pkg/std/os uses macOS-only O_* flag values; CI runs this mode on ubuntu, where Create fails (O_CREATE mistranslates to O_TRUNC). Only native_aa64 (macos-latest) is green. Real fix: compile-time target-OS awareness, NOT runtime uname — see claude-todo.md.
