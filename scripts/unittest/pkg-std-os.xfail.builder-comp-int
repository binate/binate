pkg/std/os calls __c_call, which the bytecode VM never interprets; a __c_call package runs under the VM only as the injected compiled package (registered native externs, like pkg/builtins/rt) — not wired up for os. See plan-c-call.md / claude-todo.md.
