TestExecRefIncRefDecInline hangs under boot-comp-int-int (timed out at 90s).  Three-level VM nesting (VM_outer dispatches inner cmd/bni's bytecode, which creates a fresh VM_test and runs hand-built IR in it).  Likely related to the inline-RefDec slow path or the BC_CALL extern dispatch from the inner-inner VM, but not yet diagnosed.
